# SEO Monitor — 2026-07-20

**Mode**: DAILY MONITOR (Monday, DOM=20)
**Analyst**: seo-routine (automated)
**Network note**: `brickwise.pro` is not in the session's egress allowlist — HTTP 403 on all live URL fetches. All findings are from **static source inspection** of `app/**/page.tsx` and `app/**/layout.tsx`. Live verification (canonical rendering, sitemap reachability, JSON-LD parse from browser, image dimensions) could not be completed. See "Out of scope — needs human" section.

---

## Summary

| Metric | Value |
|--------|-------|
| PublicShell routes analysed | 25 page files |
| Distinct issues found | 10 |
| Issues: HIGH | 5 |
| Issues: MEDIUM | 4 |
| Issues: LOW/INFO | 2 |
| Regression vs yesterday | N/A (no prior report — first run) |
| Passing checks | 10 |

---

## Findings

| # | Sev | Route(s) | Issue | Suggested Fix | Whitelist-safe? |
|---|-----|----------|-------|---------------|-----------------|
| 1 | HIGH | `/rankings/highest-yield` `/rankings/buy-signals` `/rankings/undervalued` `/rankings/new-listings` | Title tags 67–75 chars (≤60 required). E.g. "Highest Yield Tokenized Real Estate Properties Right Now \| Brickwise" = 67 chars. | Shorten CATEGORY_META titles in `app/rankings/[category]/page.tsx`. Examples: "Top Yield Tokenized Properties Right Now \| Brickwise" (53 ch), "Best Tokenized Real Estate to Buy Now \| Brickwise" (50 ch), "Undervalued Tokenized Properties — Below Fair Value \| Brickwise" (63 ch→shorten), "New Tokenized Real Estate Listings \| Brickwise" (47 ch). | YES |
| 2 | HIGH | `/city/[city]` (all ~30+ city pages) | Title template generates 72–85+ char titles. "Detroit Tokenized Real Estate — 120 Properties Ranked by Yield \| Brickwise" = 75 chars. | Shorten template in `app/city/[city]/page.tsx` to e.g. `${cityName} Tokenized RE: ${cityProps.length} Properties Ranked \| Brickwise` (~60 ch for short city names) or drop the count from the title. | YES |
| 3 | HIGH | `/platform/realt` `/platform/lofty` | Title ~68 chars. "RealT Properties — 350 Tokenized Real Estate Investments \| Brickwise" = 68 chars. | Shorten template in `app/platform/[platform]/page.tsx`. Example: `${platformName} Tokenized Properties — Yield & Scores \| Brickwise` (55 ch). | YES |
| 4 | HIGH | `/learn/what-is-tokenized-real-estate` `/learn/lofty-review` `/learn/realt-review` `/algorand` | Title tags 64–72 chars. "What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise" = 64 ch. "Lofty Review 2026: Yield, Fees, Risks & How It Works \| Brickwise" = 64 ch. "RealT Review 2026: Yields, Fees, Risks & Is It Worth It? \| Brickwise" = 68 ch. "Algorand Ecosystem (30+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise" = 72 ch. | Trim each title by removing secondary clauses or shortening the year placement. Examples: "What Is Tokenized Real Estate? (2026 Guide) \| Brickwise" (54 ch), "Lofty Review 2026: Yield, Fees & How It Works \| Brickwise" (57 ch), "RealT Review 2026: Yields, Fees & Is It Worth It? \| Brickwise" (61 ch — still 1 over, trim further), "Algorand Ecosystem Directory (30+ Projects) \| Brickwise" (56 ch). | YES |
| 5 | HIGH | `/learn/what-is-tokenized-real-estate` `/learn/lofty-review` `/learn/realt-review` | Meta descriptions over 160 chars (Google truncates). "Tokenized real estate converts…how to get started." = 198 ch. "Honest Lofty review…100+ properties." = 167 ch. "In-depth RealT review…compares to Lofty." = 170 ch. | Trim to 145–155 chars in `app/learn/*/page.tsx`. Cut trailing qualifying clauses. | YES |
| 6 | MEDIUM | `/property/[id]` (~484 property pages) | Meta descriptions consistently 80–110 chars, below 120-char minimum. Template: `${p.platform} property in ${p.city} — ${p.expectedYield}% net yield, ${p.overallScore}/100 Brickwise score, €${p.tokenPrice} per token.` = ~87 chars. | Extend template to include a second sentence, e.g. append `${p.fairValueStatus === 'undervalued' ? ' Token trades below fair value.' : ''} ${p.risk} risk. ${rec.action} signal.` to reach 120+ chars. Adjust in `app/property/[id]/page.tsx`. | YES |
| 7 | MEDIUM | `/` (homepage) | Meta description 117 chars, just under 120-char minimum. Template evaluates to: `${PROPERTIES.length} tokenized properties scored for yield, risk and fair value on Lofty & RealT. Buy/hold/avoid signals updated daily.` | Append a short phrase. Example: `…updated daily. No signup required.` → ~138 chars. In `app/page.tsx`. | YES |
| 8 | MEDIUM | `/compare/realt-vs-lofty` | Meta description ~119 chars, 1 char under 120 minimum (varies with PROPERTIES.length). | Append ", free." or similar trailing phrase to reach ≥120 chars. In `app/compare/realt-vs-lofty/page.tsx`. | YES |
| 9 | MEDIUM | `/algorand` | Meta description 165 chars, 5 chars over 160 maximum. "Discover 30+ projects across the Algorand ecosystem — wallets, DEXs, DeFi, NFT marketplaces, infrastructure, and developer tooling. Curated and updated by Brickwise." | Trim to ≤160 chars. Remove ", infrastructure" or ", and developer tooling" to bring it under 160. | YES |
| 10 | LOW | `/city/[city]` (all city pages) | No page-specific OG image set; inherits root layout's generic `/opengraph-image`. Missed opportunity: a per-city image with property count and yield would increase CTR from social. | Add `images: [{ url: `/api/og/city?city=${citySlug}`, ... }]` to OG metadata once a dynamic OG image endpoint exists. **Out of scope for whitelist — no `/api/` changes allowed.** | NO |

---

## Passing Checks

| Check | Result |
|-------|--------|
| `<html lang>` attribute | ✓ `lang="en"` in root layout |
| All PublicShell routes have metadata | ✓ All 25 page files verified (analyzer covered by `app/analyzer/layout.tsx`) |
| No AppShell pages in PublicShell set | ✓ AppShell found only on `watchlist`, `seo-dashboard` (not in PublicShell grep) |
| All fonts use `display: swap` | ✓ Inter, DM Mono, DM Serif Display — all `display: "swap"` in root layout |
| External font (fontshare) non-blocking | ✓ `NonBlockingStylesheet` component uses media=print swap trick |
| Global OG tags in root layout | ✓ `og:title`, `og:description`, `og:image` (/opengraph-image), `og:site_name` |
| Global Twitter card | ✓ `summary_large_image`, `@brickwisepro` in root layout |
| Canonical tags | ✓ Present on all individually inspected pages |
| Single H1 | ✓ Exactly one `<h1>` found in all inspected page JSX |
| JSON-LD static validity | ✓ All JSON-LD blocks are valid JSON in static inspection (FAQPage, Article, ItemList, BreadcrumbList, SoftwareApplication, Dataset schemas) |
| robots.ts disallows private routes | ✓ /dashboard, /portfolio, /watchlist, /settings, /sign-in, /sign-up, /crm, /growthos, /api/ all disallowed |
| robots.ts sitemap declaration | ✓ `https://brickwise.pro/sitemap.xml` declared |
| Property page title length | ✓ Template clamps name at 40 chars; resulting titles within 60 chars |
| Compare/realt-vs-lofty title | ✓ 54 chars |
| Homepage title | ✓ 56 chars |
| About title | ✓ 51 chars |
| Methodology title | ✓ 56 chars |
| Independence statement not weakened | ✓ No copy changes were made; positioning reviewed only |

---

## Out of scope — needs human

1. **Live URL verification**: `brickwise.pro` is blocked by the session's egress network policy (HTTP 403). Cannot confirm: canonical link rendering in actual HTML output, sitemap.xml reachability and URL 200 status, robots.txt content from live server, JSON-LD validity as parsed by Google, or image dimension attributes on rendered pages. Request: add `brickwise.pro` to the egress allowlist for this cron session, or manually run an audit via browser DevTools / Google Search Console.

2. **Sitemap URL 200 checks**: Cannot fetch `https://brickwise.pro/sitemap.xml` or individual property/city URLs. The sitemap is generated dynamically from `app/sitemap.ts` — logic looks correct, but live reachability is unconfirmed.

3. **robots.txt diff**: No prior reports exist (this is the first run). Cannot diff against yesterday. Baseline is now established — future runs will diff against this.

4. **City OG image** (finding #10): Requires a dynamic OG image API endpoint (`/api/og/city`) — in the blacklisted `/api/` directory. Needs a developer to implement.

5. **Image dimension attributes**: `app/page.tsx` uses `<FallbackImg>` and `<PropertyHeroImage>` components for property images loaded from external URLs (Unsplash, platform CDNs). Cannot statically verify that `width`/`height` attributes are emitted correctly. Spot-check via Lighthouse / PageSpeed.

---

## Robots.txt baseline (static source)

Disallowed paths per `app/robots.ts` (confirmed, no changes from expectations):
```
/dashboard, /portfolio, /watchlist, /settings, /sign-in, /sign-up,
/announce, /seo-dashboard, /crm, /growthos, /api/
```
CRM subdomain blocked additionally at proxy layer in `proxy.ts`.

---

*Report generated: 2026-07-20 · seo-routine v1 · static analysis only (live verification blocked)*
