# SEO Monitor Report — 2026-08-02

**Run mode:** WEEKLY FIX (DOM=02, DOW=7/Sunday)
**Audit method:** Static source-code analysis (live site returned 403 on all WebFetch attempts)
**Previous report:** None (first run)

## Summary

27 PublicShell routes audited. 9 issues found across 5 files — all in the title/description metadata layer. No structural regressions (sitemap, robots, JSON-LD schemas, canonical tags all in good shape from static review).

| Metric | Value |
|--------|-------|
| Routes checked | 27 |
| Issues found | 9 |
| Fixed this run | 9 |
| Needs human | Live verification (403 blocks) |

---

## Findings

| Sev | Route | Issue | Fix | Safe? |
|-----|-------|-------|-----|-------|
| HIGH | `/` | `title` plain string → template doubled "| Brickwise" → 59 chars shown as "…\| Brickwise \| Brickwise" | Changed to `title: { absolute: "..." }` | ✅ |
| MED | `/` | description 118 chars (min 120) | Rewrote to 142 chars | ✅ |
| HIGH | `/learn/lofty-review` | `title` plain string → template doubled → 69 chars | Changed to `title: { absolute: "Lofty Review 2026: Yield, Fees & Risks \| Brickwise" }` (51 chars) | ✅ |
| MED | `/learn/lofty-review` | description 167 chars (max 160) | Rewrote to 157 chars | ✅ |
| HIGH | `/learn/realt-review` | `title` plain string → template doubled → 71 chars | Changed to `title: { absolute: "RealT Review 2026: Yields, Fees & Risks \| Brickwise" }` (52 chars) | ✅ |
| MED | `/learn/realt-review` | description 171 chars (max 160) | Rewrote to 150 chars | ✅ |
| HIGH | `/algorand` | `title` plain string → template doubled → 72 chars after template | Changed to `title: { absolute: "Algorand Ecosystem — 33+ Projects \| Brickwise" }` (47 chars) | ✅ |
| MED | `/algorand` | description 165 chars (max 160) | Rewrote to 157 chars | ✅ |
| HIGH | `/rankings/[category]` | `generateMetadata` returned plain string title → template doubled; underlying CATEGORY_META titles 56–63 chars → final 68–75 chars | Changed to `title: { absolute: ... }` and shortened CATEGORY_META title strings to 33–35 chars (totals 45–47) | ✅ |

All 9 issues are whitelist-safe (metadata-only, no logic/component/config changes).

---

## Correctly-formed pages (no action needed)

- `/analyzer` — delegates metadata to `app/analyzer/layout.tsx` which uses `title: { absolute: ... }` ✅
- `/compare/realt-vs-lofty` — uses `title: { absolute: ... }` ✅
- `/methodology` — uses `title: { absolute: ... }` ✅
- `/about` — uses `title: { absolute: ... }` ✅
- `/property/[id]` — uses `title: { absolute: ... }` ✅
- `/city/[slug]` — checked; uses `title: { absolute: ... }` ✅
- `/market/[slug]` — checked; uses `title: { absolute: ... }` ✅
- `/algorand/[slug]` — checked; uses `title: { absolute: ... }` ✅

---

## Out of scope — needs human

| Item | Reason |
|------|--------|
| Live canonical tag verification | 403 on all WebFetch calls to brickwise.pro |
| Sitemap XML accessibility check | 403 blocked curl/fetch |
| robots.txt live diff | 403 blocked; static source looks correct |
| og:image render test | Requires live URL |
| JSON-LD validator (schema.org) | Requires live URLs |
| Core Web Vitals / LCP | Requires RUM or Lighthouse against live site |
| Google Search Console impressions delta | No GSC API access in this environment |

---

## Commits applied

1. `fix(seo): homepage title double-branding and short description` (c430da4)
2. `fix(seo): lofty-review title double-branding and description over 160 chars` (dfcb99b)
3. `fix(seo): realt-review title double-branding and description over 160 chars` (1403c78)
4. `fix(seo): algorand page title double-branding (72 chars) and description over 160` (a381901)
5. `fix(seo): rankings page title double-branding and lengths 68-75 chars` (c6f9bec)

Branch: `seo/weekly-fix-2026-08-02`
