# SEO Monitor — 2026-05-14

**Mode**: DAILY MONITOR  
**Run date**: 2026-05-14 (Thursday)  
**First run**: Yes — no prior reports to diff  
**Live-fetch status**: ❌ All WebFetch calls returned HTTP 403 (Vercel preview protection or access restriction). All findings are code-analysis only.

---

## Summary

**Pages checked (code)**: 27 PublicShell routes  
**Issues found**: 8 (2 high, 4 medium, 2 informational)  
**Regressions vs yesterday**: N/A — first run, baseline established  
**Sitemap validation**: Skipped — 403 on live fetch  
**Robots.txt diff**: N/A — baseline only  

---

## Findings Table

| Sev | Route(s) | Issue | Suggested Fix | Whitelist-safe? |
|-----|----------|-------|---------------|-----------------|
| 🔴 HIGH | `/`, `/rankings/*`, `/city/*`, `/platform/*`, `/learn/lofty-review`, `/learn/realt-review`, `/learn/what-is-tokenized-real-estate`, `/algorand`, `/compare/best-fractional-real-estate-platforms`, and other pages using plain string titles | **Potential title doubling via template**: Root layout sets `title.template: "%s | Brickwise"`. Many pages export `title: "… | Brickwise"` as a plain string (not `{ absolute: … }`). In Next.js App Router this causes the template to apply, rendering `"… | Brickwise | Brickwise"`. Cannot confirm rendered output due to 403 — but code pattern strongly suggests the bug exists. Pages correctly using `{ absolute: … }`: `/about`, `/contact`, `/privacy`, `/learn`, `/market`, `/analyzer` (layout), `/compare/realt-vs-lofty`, `/property/[id]`. | Change all string titles that already include `"\| Brickwise"` to use `title: { absolute: "…" }` form. See affected files below. | ✅ Yes — metadata exports in PublicShell pages |
| 🔴 HIGH | `/rankings/highest-yield`, `/rankings/buy-signals`, `/rankings/undervalued`, `/rankings/new-listings`, `/city/[city]`, `/platform/realt`, `/platform/lofty`, `/learn/lofty-review`, `/learn/realt-review`, `/compare/best-fractional-real-estate-platforms` | **Title length exceeds 60 chars**: Measured title strings (before template): Rankings ("Highest Yield Tokenized Real Estate Properties Right Now \| Brickwise" = 67 chars); City ("{City} Tokenized Real Estate — N Properties Ranked by Yield \| Brickwise" = 71–83 chars depending on city); Platform ("RealT Properties — N Tokenized Real Estate Investments \| Brickwise" ≈ 69 chars); Lofty review (65 chars); RealT review (69 chars); Best-fractional-platforms (75 chars). Google truncates above 60. | Shorten titles. Rankings: drop "Properties Right Now", use shorter pattern. City: use "{City} Tokenized Real Estate — N Properties \| Brickwise". Platform: "{Platform}: N Tokenized Properties \| Brickwise". Reviews: "Lofty Review 2026: Yields, Fees & Verdict \| Brickwise" (54 chars). | ✅ Yes — metadata exports in PublicShell pages |
| 🟡 MED | `/privacy` | **Description 3 chars under 120-char minimum**: "How Brickwise handles your data. We don't sell personal data. Cookies are used for sign-in and product analytics only." = ~117 chars. | Expand by a few words, e.g. add "or third parties." at end. | ✅ Yes |
| 🟡 MED | `/learn/lofty-review`, `/learn/realt-review`, `/learn/what-is-tokenized-real-estate`, `/algorand`, `/rankings/*` | **No page-specific Twitter metadata**: These pages do not define a `twitter:` key in their metadata object. They inherit the root layout's generic Twitter card (`title: "Brickwise — Tokenized Real Estate Analytics"`). The page-specific title and description will NOT appear in Twitter embeds for these pages. | Add `twitter: { card: "summary_large_image", title: "...", description: "..." }` to each page's metadata. | ✅ Yes |
| 🟡 MED | `/about`, `/contact` | **No page-specific Twitter metadata** (same as above): About and Contact pages inherit generic Twitter card from root layout. | Same fix as above — add `twitter` key to page metadata. | ✅ Yes |
| 🟡 MED | `/contact` | **No openGraph.images**: Contact page metadata defines `og:title` and `og:description` but not `og:images`. Will fall back to root layout's generic `/opengraph-image`. Acceptable but sub-optimal for sharing. | Add `images: [{ url: "/opengraph-image", width: 1200, height: 630, alt: "Contact Brickwise" }]` to openGraph in contact metadata. | ✅ Yes |
| ℹ️ INFO | `/algorand` | **Title potentially long**: `"Algorand Ecosystem (N+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise"` length depends on `ALGORAND_TOTAL_COUNT`. If N ≥ 2 digits (≥10 projects) this is ≥73 chars. Confirm count before raising severity. | Shorten to `"Algorand Ecosystem: N+ Projects Tracked \| Brickwise"`. | ✅ Yes |
| ℹ️ INFO | All routes | **Live HTML validation not possible**: brickwise.pro returns HTTP 403 on all fetch attempts. Cannot validate: rendered title/description/canonical in actual HTML, sitemap URL reachability, robots.txt content, OG/Twitter tag rendering, JSON-LD parse validity, `<html lang>` in rendered output. | Human: verify https://brickwise.pro is publicly accessible from external IPs; check if Vercel protection is enabled on production. Once accessible, re-run monitor with live fetch. | ❌ No — out of automated scope |

---

## Affected files for HIGH finding #1 (title doubling)

These files export `title` as a plain string including `"| Brickwise"` and need `{ absolute: "…" }` wrapping:

| File | Current title (truncated) | Recommended action |
|------|--------------------------|-------------------|
| `app/page.tsx` | `"Tokenized Real Estate Scored — Lofty & RealT \| Brickwise"` | Wrap in `{ absolute: "…" }` |
| `app/rankings/[category]/page.tsx` | `\`${meta.title} \| Brickwise\`` | Wrap in `{ absolute: \`${meta.title} \| Brickwise\` }` AND shorten |
| `app/platform/[platform]/page.tsx` | `\`${platformName} Properties — … \| Brickwise\`` | Wrap in `{ absolute: "…" }` AND shorten |
| `app/city/[city]/page.tsx` | `\`${cityName} Tokenized Real Estate — … \| Brickwise\`` | Wrap in `{ absolute: "…" }` AND shorten |
| `app/learn/lofty-review/page.tsx` | `"Lofty Review 2026: Yield, Fees, Risks & How It Works \| Brickwise"` | `{ absolute: "Lofty Review 2026: Yields, Fees & Verdict \| Brickwise" }` |
| `app/learn/realt-review/page.tsx` | `"RealT Review 2026: Yields, Fees, Risks & Is It Worth It? \| Brickwise"` | `{ absolute: "RealT Review 2026: Yields, Fees & Verdict \| Brickwise" }` |
| `app/learn/what-is-tokenized-real-estate/page.tsx` | `"What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise"` | `{ absolute: "What Is Tokenized Real Estate? Guide (2026) \| Brickwise" }` |
| `app/algorand/page.tsx` | `\`Algorand Ecosystem (N+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise\`` | `{ absolute: \`Algorand Ecosystem: N+ Projects Tracked \| Brickwise\` }` |
| `app/compare/best-fractional-real-estate-platforms/page.tsx` | `"Best Fractional Real Estate Platforms (2026): Honest Comparison \| Brickwise"` | `{ absolute: "Best Fractional Real Estate Platforms 2026 \| Brickwise" }` |

Note: `/compare/realt-vs-lofty`, `/about`, `/contact`, `/privacy`, `/learn`, `/market`, `/analyzer` (layout), and `/property/[id]` already use `{ absolute: "…" }` correctly.

---

## Root-layout observed (baseline)

**`app/robots.ts`** disallows: `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`. Sitemap declared as `https://brickwise.pro/sitemap.xml`. No changes observed (first run baseline).

**Fonts**: Inter, DM_Mono, DM_Serif_Display all use `display: "swap"` via next/font/google ✓. External fontshare.com font loaded non-blocking with `display=swap` param and media=print swap pattern ✓.

**`<html lang="en">`**: Set in root layout ✓.

**Clerk placement**: Inside `<body>`, not `<html>` ✓.

**JSON-LD presence**: Homepage (FAQPage + ItemList), Analyzer layout (WebPage + SoftwareApplication), About (AboutPage), Contact (ContactPage), Learn (CollectionPage), RealT vs Lofty (Article + FAQPage + BreadcrumbList), Platform pages (CollectionPage), Rankings (ItemList + BreadcrumbList), City pages (Dataset), Lofty Review (Review + Article + FAQPage + BreadcrumbList), RealT Review (Article + FAQPage + BreadcrumbList), Algorand (CollectionPage + BreadcrumbList), Market (no JSON-LD detected). All JSON-LD blocks use `JSON.stringify()` output — format is correct but parse validity unverifiable without live fetch.

**Market page** — no JSON-LD block in rendered JSX. Minor gap for a data-rich page.

---

## Spot-check: 3 random pages for images/fonts/CLS

**Live fetch blocked (403)** — cannot inspect rendered DOM. Code-level observations only:

1. **`app/page.tsx`** (homepage): Uses `<FallbackImg>` component (custom component, not next/image). Cannot verify `width`/`height` attributes from code alone — needs live inspection or component source review. ⚠️ Flagged for human follow-up.

2. **`app/market/page.tsx`**: Uses `<FallbackImg>` on property cards without explicit dimensions visible in JSX (`className="w-9 h-9 ..."`). CSS-based sizing only — no HTML width/height attributes means potential CLS. ⚠️

3. **`app/algorand/page.tsx`**: Uses `<ProjectLogo>` component with `size={48}` prop — likely renders with explicit dimensions. Low CLS risk if component passes size to underlying `<img>`. ✓ assumed.

---

## Out of scope — needs human

| Issue | Why out of scope |
|-------|-----------------|
| HTTP 403 on https://brickwise.pro | Can't determine if production is behind Vercel protection or misconfigured. Blocks all live validation including sitemap, robots.txt, HTML audit. |
| Title template behavior in Next.js 16 | AGENTS.md warns of breaking API changes. Title doubling finding assumes standard Next.js behavior. If this version changed how `title.template` interacts with string titles, finding #1 may not apply. Requires a human to view page source on the live site. |
| `<FallbackImg>` component width/height behaviour | Custom component not in whitelist — cannot inspect or modify. Needs human to verify it emits `width`/`height` on `<img>`. |
| Market page missing JSON-LD | Missing structured data on `/market` — out of whitelist scope (no JSON-LD block to edit, would require adding new code). |
| Image CLS across property cards | CLS can only be measured in browser (Chrome DevTools / CrUX). Cannot audit from code. |
