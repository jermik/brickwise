# SEO Monitor — 2026-07-24 (Daily)

**Run date:** 2026-07-24 | **Mode:** DAILY MONITOR (read-only)  
**Branch:** `seo/monitor-2026-07-24`  
**Live verification:** ❌ blocked — `brickwise.pro` returns HTTP 403 from org egress policy; all findings are source-code only.

---

## Summary

| Metric | Value |
|--------|-------|
| PublicShell routes enumerated | 24 (static) + property/city/algorand/market/rankings (dynamic) |
| Issues found | 9 |
| HIGH severity | 3 |
| MEDIUM severity | 4 |
| LOW severity | 2 |
| Regression vs yesterday | No baseline; first monitor run |

---

## Findings

| # | Severity | Route(s) | Issue | Suggested Fix | Whitelist-safe? |
|---|----------|----------|-------|---------------|-----------------|
| 1 | **HIGH** | `/` · `/learn/*` · `/rankings/*` · `/city/*` · `/platform/*` · `/algorand` · `/algorand/*` · `/market/*` · `/compare/best-*` · `/compare/*-vs-*` (excl. `realt-vs-lofty`) — ~19 page templates | **Title doubling** — `app/layout.tsx` sets `title.template: "%s | Brickwise"`. All pages that export a plain string containing `\| Brickwise` get rendered in-browser as `… \| Brickwise \| Brickwise`. Browser title lengths balloon to 76–91 chars against a ≤60 target. | Change all affected `generateMetadata` / `export const metadata` title values to `title: { absolute: "…" }`. Pages already using `absolute` (e.g. `/compare/realt-vs-lofty`, `/methodology`, `/market`) are unaffected and are the correct reference pattern. | ✅ Yes — metadata-only edits in PublicShell pages |
| 2 | **HIGH** | `/analyzer` | **No metadata at all** — `app/analyzer/page.tsx` is a `"use client"` component and therefore cannot export `metadata`. The page inherits the layout default title ("Brickwise — Tokenized Real Estate Investment Analytics"), no `/analyzer` canonical, and no page-specific description. Priority 0.9 in sitemap, so this is a high-value gap. | Extract the non-interactive shell and metadata into a Server Component wrapper (e.g. `app/analyzer/layout.tsx`) that exports `metadata`, and keep the interactive content in a nested client component. | ✅ Yes — adding a layout.tsx is metadata-only scaffolding |
| 3 | **HIGH** | `/methodology` | **Missing from sitemap** — `app/methodology/page.tsx` exists with correct `{ absolute }` title, description, and canonical, but `app/sitemap.ts` has no entry for `/methodology`. Googlebot will only find it via crawl links, not via the sitemap. | Add `{ url: \`${base}/methodology\`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.7 }` to the `staticRoutes` array in `app/sitemap.ts`. | ✅ Yes — sitemap.ts edit |
| 4 | **MEDIUM** | `/learn/what-is-tokenized-real-estate` | **Description too long** — 198 chars (limit: 120–160). Will be truncated to ~155 chars in SERPs with an ellipsis mid-sentence. | Trim to ≤155 chars while keeping primary keyword "tokenized real estate" in the first 120 chars. | ✅ Yes |
| 5 | **MEDIUM** | `/city/[city]` (all city pages) | **Description too long** — Detroit example: ~166 chars. Pattern: `` `Explore ${n} tokenized rental properties in ${city} on RealT and Lofty…` `` runs long for cities with many properties. | Cap template at 155 chars with a slice + `…` guard. | ✅ Yes |
| 6 | **MEDIUM** | `/property/[id]` (all property pages) | **Description too short** — most generated descriptions are ~86 chars against the 120-char minimum. The generator in `app/property/[id]/page.tsx` caps at 129 chars (`desc.slice(0, 129) + "…"`) but the actual descriptions are short at source, so the cap rarely helps. | Enrich the description template to guarantee ≥120 chars, e.g. append city + platform + yield context if the source string is under 120. | ✅ Yes |
| 7 | **MEDIUM** | `/` (homepage) | **Description marginally short** — ~118 chars (minimum 120). Currently: dynamic interpolation of `PROPERTIES.length` + fixed suffix. Just 2 chars under the floor. | Add 2–5 words to the fixed suffix (e.g. "…updated daily." → "…updated daily across all listings.") | ✅ Yes |
| 8 | LOW | `/property/[id]` | **Title slightly long** — uses `{ absolute }` correctly (no doubling), but the resulting string runs 65–67 chars for typical property names, above the 60-char recommendation. `shortName` is clamped at 40 chars but the pattern `${shortName} — ${yield}% Yield \| Brickwise` still exceeds 60. | Clamp `shortName` at 30 chars instead of 40, or shorten the suffix to `\| BW`. | ✅ Yes |
| 9 | LOW | `/algorand` | **Description slightly long** — ~165 chars. | Trim by ~10 chars. | ✅ Yes |

---

## Out of scope — needs human

| Item | Why it needs human review |
|------|--------------------------|
| **Analyzer page restructure (issue #2)** | While a `layout.tsx` wrapper is technically within scope, restructuring a `"use client"` page requires a dev review of the interactive component tree to ensure no hydration regressions. Flagged here; the fix itself is low-risk but should be verified by building locally. |
| **Live SERP verification** | All findings are source-code only. Live title rendering, canonical header presence, and actual SERP snippet length cannot be confirmed without access to `brickwise.pro` (blocked by org egress policy in this environment). Recommend running a Screaming Frog or Search Console crawl to confirm title doubling is live and not already patched server-side. |
| **Search Console impressions delta** | No GSC data accessible. Cannot determine whether title doubling or description lengths are currently depressing CTR vs prior weeks. |

---

## Reference: pages using `{ absolute }` correctly (no action needed)

- `/compare/realt-vs-lofty` — 54 chars ✓
- `/methodology` — 56 chars ✓ (add to sitemap, see issue #3)
- `/about` — 51 chars ✓
- `/market` (index) — 49 chars ✓
- `/learn` (index) — 58 chars ✓
- `/property/[id]` — absolute, 65–67 chars (slightly long, see issue #8)

---

## Reference: robots.txt

No issues found. Disallow list is correct for all private/auth routes. Sitemap URL points to `https://brickwise.pro/sitemap.xml`. ✓

---

## Reference: technical SEO (source-level)

| Check | Status |
|-------|--------|
| `<html lang="en">` | ✓ |
| `metadataBase` set | ✓ `https://brickwise.pro` |
| OG tags present on key pages | ✓ |
| Twitter card tags | ✓ |
| JSON-LD schemas | ✓ FAQPage, Article, Dataset, ItemList, BreadcrumbList, Organization, WebSite |
| font-display: swap (Inter, DM_Mono, DM_Serif_Display) | ✓ |
| font-display: swap (Fontshare external) | ✓ `?display=swap` param in URL |
| Canonical set per-page | ✓ on all audited pages except /analyzer |

---

*Report generated by automated SEO monitor. Source-code only — live HTTP verification blocked by org egress policy. Not financial advice.*
