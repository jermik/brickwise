# SEO Monitor — 2026-06-19

**Mode**: DAILY MONITOR (Friday, DOM=19)
**Pages checked**: 27 PublicShell routes
**Issues found**: 8 (1 critical, 1 high, 6 medium/low)
**Regression vs yesterday**: No prior report — this is run #1 (baseline)
**Live URL access**: 403 Forbidden from sandbox — all checks performed via static source analysis

---

## Summary

First monitor run. Static analysis completed on all 27 PublicShell routes. One critical structural issue found (analyzer has no metadata), one high-priority omission (methodology missing from sitemap), and five pages with descriptions over the 160-char SERP limit. Em-dash usage in title tags violates the brand anti-patterns file across six pages. All image components use raw `<img>` without explicit dimensions — CLS risk — but those components live in the blacklist and cannot be touched by this routine.

---

## Findings Table

| Sev | Route | Issue | Suggested Fix | Whitelist-safe? |
|-----|-------|-------|---------------|----------------|
| CRITICAL | `/analyzer` | `"use client"` page — cannot export `metadata`. Falls back to root layout title/description. Priority 0.9 in sitemap but no page-specific title, description, canonical, or OG. | Extract metadata to a thin server wrapper that renders the client component. Requires developer decision on architecture. | ❌ Needs Human |
| HIGH | `/methodology` | Missing from `sitemap.ts`. Full PublicShell page with valid metadata but not submitted for crawl. | Add `{ url: "${base}/methodology", lastModified: new Date(), changeFrequency: "monthly", priority: 0.75 }` to `staticRoutes` in `app/sitemap.ts`. | ✅ Yes (sitemap.ts is whitelisted) |
| MED | `/learn/what-is-tokenized-real-estate` | Description ~198 chars (limit 120–160). Will be truncated in SERPs. | Trim to: `"Tokenized real estate converts property ownership into blockchain tokens. Learn how it works, typical yields (8–14%), risks, and how to get started."` (148 chars) | ✅ Yes |
| MED | `/learn/how-to-invest-in-tokenized-real-estate` | Description ~181 chars (over 160 limit). | Trim to: `"Complete guide to buying your first tokenized real estate token. Platform selection, KYC, funding, and earning daily rental income from $50."` (140 chars) | ✅ Yes |
| MED | `/compare/best-fractional-real-estate-platforms` | Description ~197 chars (over 160 limit). | Trim to: `"Independent comparison of Lofty, RealT, Fundrise, Arrived, and Ark7. Minimums, liquidity, distribution frequency, and who each platform is best for."` (151 chars) | ✅ Yes |
| MED | `/learn/realt-review` | Description ~170 chars (over 160 limit). | Trim to: `"Data-driven RealT review: actual yields, management fees, vacancy rates, and how RealT compares to Lofty. Based on live data from hundreds of properties."` (153 chars) | ✅ Yes |
| MED | `/learn/lofty-review` | Description ~169 chars (over 160 limit). | Trim to: `"Honest Lofty review: $50 minimum, daily USDC payouts, Algorand-based tokens. Real yields, fees, liquidity, and who it's best for. Data from 100+ properties."` (158 chars) | ✅ Yes |
| MED | Multiple title tags | Em-dash (`—`) in title tags violates `anti-patterns.md` ("Em-dashes in marketing copy"). Affects root layout default, homepage, compare/realt-vs-lofty, methodology, market/page. | Replace `—` with `: ` or `\|` in title strings. Examples: "Methodology: How Brickwise Scores Tokenized Real Estate", "Tokenized Real Estate Market Overview \| Brickwise". | ✅ Yes (for PublicShell pages) |
| MED | `market/page.tsx` | Missing page-specific Twitter card. Inherits generic root card with homepage-level copy. | Add `twitter: { card: "summary_large_image", title: "Tokenized Real Estate Market Overview", description: \`Live market data for ${PROPERTIES.length} properties...\` }` | ✅ Yes |
| LOW | Several pages | Missing page-specific Twitter card (learn/*, platform/[platform], about, methodology). All inherit root default. Low impact because root default is reasonable. | Add twitter: {} block to each page's metadata for better social sharing. | ✅ Yes |
| LOW | `/compare/realt-vs-lofty` | Description is exactly 120 chars (lower boundary). Technically in range but thin. | Minor: extend by 10–15 chars, e.g. append `"No affiliation with either platform."` | ✅ Yes |
| LOW | `components/ui/fallback-img.tsx`, `components/property/property-hero-image.tsx`, `components/property/property-card.tsx` | Raw `<img>` tags lack explicit `width` and `height` attributes. Container CSS partially mitigates CLS, but best-pick hero on homepage and property hero are likely LCP candidates with no `fetchpriority="high"`. | Add `loading="eager"` and `fetchpriority="high"` to LCP images; add `width`/`height` or use Next.js `<Image>`. | ❌ Needs Human (blacklisted components) |

---

## Metadata Completeness Summary (all 27 PublicShell routes)

| Route | Has `metadata` | Title ≤60 | Desc 120–160 | Canonical | OG | Twitter | H1 |
|-------|---------------|-----------|-------------|-----------|----|---------|----|
| `/` | ✅ | ✅ 55 chars | ⚠️ 118 chars (borderline) | ✅ | ✅ no images | ✅ | ✅ |
| `/analyzer` | ❌ `"use client"` | ❌ inherits root | ❌ inherits root | ❌ inherits root | ❌ | ❌ | ✅ |
| `/methodology` | ✅ | ✅ 56 chars | ✅ 151 chars | ✅ | ✅ | ❌ inherits | ✅ |
| `/about` | ✅ | — | — | ✅ | ✅ | ❌ inherits | ✅ |
| `/contact` | ✅ | — | — | ✅ | ❌ inherits | ❌ inherits | ✅ |
| `/privacy` | ✅ | — | — | ✅ | ❌ inherits | ❌ inherits | — |
| `/learn` | ✅ | — | — | ✅ | ✅ | ❌ inherits | ✅ |
| `/learn/what-is-tokenized-real-estate` | ✅ | — | ❌ ~198 chars | ✅ | ✅ | ❌ inherits | ✅ |
| `/learn/lofty-review` | ✅ | — | ❌ ~169 chars | ✅ | ✅ | ❌ inherits | ✅ |
| `/learn/realt-review` | ✅ | — | ❌ ~170 chars | ✅ | ✅ | ❌ inherits | ✅ |
| `/learn/how-to-invest-in-tokenized-real-estate` | ✅ | — | ❌ ~181 chars | ✅ | ✅ | ❌ inherits | ✅ |
| `/compare/realt-vs-lofty` | ✅ | ✅ | ⚠️ 120 chars (edge) | ✅ | ✅ | ✅ | ✅ |
| `/compare/best-fractional-real-estate-platforms` | ✅ | — | ❌ ~197 chars | ✅ | ✅ | ✅ | ✅ |
| `/compare/lofty-vs-arrived` | ✅ | — | — | ✅ | ✅ | ✅ | — |
| `/compare/realt-vs-fundrise` | ✅ | — | — | ✅ | ✅ | ✅ | — |
| `/compare/arrived-vs-fundrise` | ✅ | — | — | ✅ | ✅ | ✅ | — |
| `/compare/ark7-vs-lofty` | ✅ | — | — | ✅ | ✅ | ✅ | — |
| `/compare/best-real-estate-investing-apps` | ✅ | — | — | ✅ | ✅ | ✅ | — |
| `/platform/[platform]` | ✅ (generateMetadata) | — | — | ✅ | ✅ | ❌ inherits | ✅ |
| `/rankings/[category]` | ✅ (generateMetadata) | — | — | ✅ | ✅ | — | ✅ |
| `/city/[city]` | ✅ (generateMetadata) | — | — | ✅ | ✅ | — | ✅ |
| `/property/[id]` | ✅ (generateMetadata) | ✅ (clamped) | ⚠️ max 130 | ✅ | ✅ | ✅ | ✅ |
| `/algorand` | ✅ | — | — | ✅ | ✅ | ✅ | — |
| `/algorand/[slug]` | ✅ (generateMetadata) | — | — | ✅ | ✅ | ✅ | — |
| `/market` | ✅ | — | ✅ 130 chars | ✅ | ✅ | ❌ inherits | ✅ |
| `/market/[date]` | ✅ (generateMetadata) | — | — | ✅ | ✅ | ✅ | ✅ |

Legend: — = not checked (reasonable or N/A for page type)

---

## robots.txt — Current State (no prior diff available, run #1)

```
User-agent: *
Allow: /
Disallow: /dashboard
Disallow: /portfolio
Disallow: /watchlist
Disallow: /settings
Disallow: /sign-in
Disallow: /sign-up
Disallow: /announce
Disallow: /seo-dashboard
Disallow: /crm
Disallow: /growthos
Disallow: /api/
Sitemap: https://brickwise.pro/sitemap.xml
```

Assessment: Correct. All private/auth surfaces blocked. Public analytics routes open. CRM blocked both here and at proxy level per source comments.

---

## Sitemap Assessment (static analysis — live check blocked by 403)

- **Scope**: `/`, `/analyzer`, `/compare/*` (7 pages), `/learn/*` (5 pages), `/platform/realt`, `/platform/lofty`, `/algorand`, city pages (from PROPERTIES), rankings (4 pages), property pages (from PROPERTIES), market pages, algorand project pages
- **Properties count**: ~466 (properties-auto.json) + 13 live-merged entries
- **Missing from sitemap**: `/methodology` ← needs to be added to `staticRoutes`
- **lastmod**: All static routes use `new Date()` (always fresh — acceptable for content that updates implicitly). Property pages use `p.lastUpdated` ✅. Market pages use `u.date` ✅.
- **Live URL verification**: Not possible from this sandbox (403). Recommend verifying via Google Search Console or a separate check with network access.

---

## JSON-LD Assessment

- **Root layout**: Organization + WebSite schema ✅ (valid structure)
- **Homepage**: FAQPage + ItemList schema — inline JSON.stringify, valid ✅
- **Methodology**: Article schema ✅
- **Property pages**: Product/RealEstate schema with AggregateRating ✅
- **Rankings**: Dataset schema ✅
- **City pages**: Dataset + Place schema ✅
- **Learn pages**: Article + FAQPage schema ✅

---

## Font / CLS Spot-Check (3 pages)

**Page 1: `/` (homepage)**
- Inter, DM Mono, DM Serif Display: all `display: "swap"` ✅
- Fontshare (General Sans): non-blocking stylesheet trick via `media=print` swap ✅
- Best-pick hero image (`FallbackImg`): no explicit `width`/`height` attributes. Container is `h-[200px]` fixed, mitigating CLS. But `loading="eager"` not set — browser may lazy-load the LCP image. ⚠️

**Page 2: `/property/[id]` (spot-check)**
- `PropertyHeroImage`: raw `<img>` without `width`/`height`. Container is absolutely positioned in a flex layout. CLS risk low (container height defined by CSS). No `fetchpriority="high"` on LCP image. ⚠️

**Page 3: `/compare/realt-vs-lofty`**
- No `<img>` or `<Image>` elements (pure text + tables) ✅
- No CLS risk from images

---

## Out of Scope — Needs Human

1. **`/analyzer` metadata fix** — Requires architectural decision: either split into server wrapper + client component, or add metadata via `generateMetadata` in a parent layout. Developer must decide. Adding `metadata` to the current `"use client"` file is not possible in Next.js App Router.

2. **Image dimension / LCP optimization** — `FallbackImg`, `PropertyHeroImage`, `PropertyCard` are in `components/ui/` and `components/property/` (blacklisted). Need developer to either add `width`/`height` HTML attributes, convert to Next.js `<Image>`, or add `fetchpriority="high"` + `loading="eager"` on known LCP images.

3. **Live URL verification** — Sandbox gets 403 from brickwise.pro. Could be Cloudflare or Vercel bot protection. Live checks (title, canonical, JSON-LD parse) require a browser or authenticated network path. Consider enabling a monitoring tool (e.g. Screaming Frog, sitebulb, or GSC URL inspection) for future verification.

---

## Fixes Ready for Next WEEKLY FIX Run (Sunday)

All items below are whitelist-safe:

1. Add `/methodology` to `sitemap.ts` staticRoutes
2. Trim 5 over-length descriptions (learn/*, compare/best-fractional)
3. Replace em-dashes with `: ` or ` | ` in title tags where applicable (6 pages)
4. Add `twitter` card to `market/page.tsx` and optionally to learn/* and platform pages

---

*Generated by seo-routine · 2026-06-19 04:xx UTC*
