# SEO Monitor — 2026-05-15

**Mode**: DAILY MONITOR (DOW=5/Friday, DOM=15)  
**Run**: First ever — no prior report baseline, no regression comparison possible.  
**Live-URL check**: All WebFetch calls to `brickwise.pro` returned HTTP 403 Forbidden. Audit is **source-code only**; findings reflect `metadata` exports and `app/robots.ts` / `app/sitemap.ts` as committed. Rendered-HTML verification (actual `<title>`, `<h1>` count, canonical in DOM, image alt attributes) requires live access — flagged below.

---

## Summary

| Stat | Value |
|---|---|
| PublicShell pages audited (code) | 17 static routes + 4 dynamic route patterns |
| Distinct issues found | 14 |
| Regressions vs yesterday | N/A — first run |
| Live HTML verified | ❌ 403 on all fetches |

---

## Findings Table

| # | Sev | Route | Issue | Suggested Fix | Whitelist-safe? |
|---|---|---|---|---|---|
| 1 | HIGH | `/learn/how-to-invest-in-tokenized-real-estate` | Title 77 chars — exceeds 60-char limit by 17 | Shorten to ≤ 60, e.g. `"How to Invest in Tokenized Real Estate (2026) \| Brickwise"` (57 chars) | ✅ yes |
| 2 | HIGH | `/compare/ark7-vs-lofty` | Title 79 chars — exceeds limit by 19 | e.g. `"Ark7 vs Lofty (2026): Tokenized RE Comparison \| Brickwise"` (57 chars) | ✅ yes |
| 3 | HIGH | `/compare/best-real-estate-investing-apps` | Title 76 chars — exceeds limit by 16 | e.g. `"Best Real Estate Investing Apps 2026 \| Brickwise"` (49 chars) | ✅ yes |
| 4 | HIGH | `/compare/best-fractional-real-estate-platforms` | Title 75 chars — exceeds limit by 15 | e.g. `"Best Fractional Real Estate Platforms 2026 \| Brickwise"` (55 chars) | ✅ yes |
| 5 | HIGH | `/compare/lofty-vs-arrived` | Title 75 chars — exceeds limit by 15 | e.g. `"Lofty vs Arrived (2026): Token vs Rental \| Brickwise"` (53 chars) | ✅ yes |
| 6 | HIGH | `/compare/arrived-vs-fundrise` | Title 75 chars — exceeds limit by 15 | e.g. `"Arrived vs Fundrise (2026): Rental vs eREIT \| Brickwise"` (56 chars) | ✅ yes |
| 7 | HIGH | `/compare/realt-vs-fundrise` | Title 73 chars — exceeds limit by 13 | e.g. `"RealT vs Fundrise (2026): Tokenized vs eREIT \| Brickwise"` (57 chars) | ✅ yes |
| 8 | HIGH | `/learn/realt-review` | Title 68 chars — exceeds limit by 8 | e.g. `"RealT Review 2026: Yields, Fees & Risks \| Brickwise"` (52 chars) | ✅ yes |
| 9 | MED | `/algorand` | Title 72 chars (with count=33) — exceeds limit by 12 | Trim: `"Algorand Ecosystem — 33+ Projects: DeFi, Wallets, RWA \| Brickwise"` (67) still long; simplify to `"Algorand Ecosystem Projects — DeFi, NFTs, RWA \| Brickwise"` (59 chars) | ✅ yes |
| 10 | MED | `/learn/what-is-tokenized-real-estate` | Title 64 chars — exceeds limit by 4 | e.g. `"What Is Tokenized Real Estate? Guide 2026 \| Brickwise"` (54 chars) | ✅ yes |
| 11 | MED | `/learn/lofty-review` | Title 64 chars — exceeds limit by 4 | e.g. `"Lofty Review 2026: Yields, Fees & Risks \| Brickwise"` (52 chars) | ✅ yes |
| 12 | MED | `/platform/[platform]` | Dynamic title ~68 chars for RealT — exceeds limit | e.g. `"${platformName} Tokenized Properties — Ranked \| Brickwise"` (~57 chars) | ✅ yes |
| 13 | MED | `/market/[date]` | `openGraph` block has no `url` field — inherits root `https://brickwise.pro`. All market update pages show wrong OG URL when shared | Add `url: \`https://brickwise.pro/market/${u.slug}\`` to openGraph block | ✅ yes |
| 14 | LOW | `/` and `/privacy` | Meta description 118 chars — 2 chars under the 120-char minimum | Append 2+ chars, e.g. home: `"…signals updated daily."` → `"…signals, updated daily."` (same content, different punctuation adds room) | ✅ yes |
| 15 | LOW | `/algorand` description | ~165 chars — 5 chars over the 160-char maximum | Trim trailing phrase; remove `"Curated and updated by Brickwise."` (saves 34 chars) | ✅ yes |
| 16 | LOW | Most `compare/*`, `learn/*`, `algorand`, `platform/[platform]`, `city/[city]`, `rankings/[category]`, `market`, `market/[date]` | No explicit `twitter` card in page metadata — all inherit generic root layout titles ("Brickwise — Tokenized Real Estate Analytics") regardless of page content. Social shares show wrong title | Add `twitter: { card: "summary_large_image", title: "...", description: "..." }` matching each page's primary keyword | ✅ yes (metadata in PublicShell pages) |
| 17 | LOW | `app/sitemap.ts` — static routes | `lastModified: new Date()` for all static routes (home, compare/*, learn/*, algorand, about, contact, privacy). Every crawl reports them as modified today, even if unchanged. May inflate crawl budget | Use committed `git log` dates or a static date constant for infrequently-changing pages | ✅ yes (sitemap.ts is on whitelist) |

---

## Checks Verified From Source

| Check | Result |
|---|---|
| Every PublicShell page exports `metadata` | ✅ Yes — `analyzer/page.tsx` is `"use client"` but delegates to `analyzer/layout.tsx` which exports metadata |
| Root layout `<html lang="en">` | ✅ Present |
| Fonts load with `font-display: swap` | ✅ All three Google Fonts (Inter, DM_Mono, DM_Serif_Display) set `display: "swap"` |
| General Sans (Fontshare) async loading | ✅ `NonBlockingStylesheet` + `media=print` trick + `<noscript>` fallback |
| Canonical link present | ✅ All static and dynamic PublicShell pages set `alternates.canonical` |
| `robots.ts` disallows private routes | ✅ Disallows `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/` |
| Open Graph tags present | ✅ All pages have `openGraph` block; `metadataBase` set in root layout |
| JSON-LD syntax validity | ✅ All inline JSON-LD blocks reviewed appear structurally valid |
| `sitemap.xml` registered in robots | ✅ `sitemap: "https://brickwise.pro/sitemap.xml"` present |
| `host` set in robots | ✅ `host: "https://brickwise.pro"` |
| Global Organization + WebSite schema | ✅ In root `layout.tsx` |

---

## Checks NOT Verified (live access blocked)

The following require a human or a network-accessible agent to confirm:

- Rendered `<title>` text (verifying Next.js metadata output matches source)
- `<meta name="description">` as rendered (Next.js may strip/truncate)
- Exactly one `<h1>` on rendered pages (source has single H1 in each page reviewed, but cannot confirm full set including dynamic pages)
- Canonical link in rendered `<head>` matches expected URL
- Image dimensions / missing `width`/`height` on `<img>` tags causing layout shift
- `<meta name="robots">` as rendered per-page
- Sitemap reachability and HTTP 200 for all listed URLs

---

## Robots.txt — Current State (no prior baseline)

```
User-agent: *
Allow: /
Disallow: /dashboard
Disallow: /portfolio
Disallow: /watchlist
Disallow: /settings
Disallow: /sign-in
Disallow: /sign-up
Disallow: /announce
Disallow: /seo-dashboard
Disallow: /crm
Disallow: /growthos
Disallow: /api/
Sitemap: https://brickwise.pro/sitemap.xml
Host: https://brickwise.pro
```

*First run — no diff possible. Will diff against this on next run.*

---

## Out of Scope — Needs Human

1. **Live HTTP verification of all sitemap URLs** — sitemap includes property routes (463 entries), city routes, market/[date] routes. Must be checked against actual Vercel deployment. Cannot be done from this environment (403 on all fetches).
2. **Image `width`/`height` audit** — `FallbackImg` component is used across property pages. Dimensions must be verified in rendered HTML to confirm no layout shift.
3. **Core Web Vitals (INP, LCP, CLS)** — cannot be measured from source. Requires live PageSpeed Insights run.
4. **`/methodology` route** — the home page links to `/methodology` (`<Link href="/methodology">`), but this route does **not** appear in `app/sitemap.ts` and was not found in the PublicShell grep. If this route exists, it should be added to the sitemap. If it doesn't exist, the internal link is broken. **Needs human investigation.**
5. **`/growthos` internal landing** — disallowed in robots but listed as a route. Confirm it is not accidentally indexed anywhere via external links.
