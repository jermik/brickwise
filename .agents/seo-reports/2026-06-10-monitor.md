# SEO Monitor — 2026-06-10

**Mode**: DAILY MONITOR (Wednesday, DOM=10)
**Run by**: seo-routine (automated)
**Baseline**: First run — no prior report to diff against

---

## Summary

Pages identified: **26 PublicShell routes** (including dynamic templates covering 460+ properties, 30+ Algorand projects, 40+ cities, 4 ranking categories, multiple market reports)

Issues found: **2 critical, 4 high, 2 medium, 1 blocker**

Regression vs yesterday: No baseline — first run.

**Top finding**: A systematic double `| Brickwise` title suffix affects ~22 pages. Pages that set `title` as a plain string ending in `| Brickwise` have the root-layout template (`%s | Brickwise`) applied on top, producing `… | Brickwise | Brickwise` in the rendered `<title>`. This is likely suppressing SERP CTR on the majority of high-priority pages. All fixes are whitelist-safe.

**Blocker**: Live site (`brickwise.pro`) returned HTTP 403 on every request from this sandbox. All findings below are from **static source analysis only**. Live-rendered title output, sitemap reachability, and robots.txt content could not be verified. A human must run live checks from an unrestricted network or add the sandbox egress IP to any WAF allowlist.

---

## Findings Table

| # | Severity | Route(s) | Issue | Suggested Fix | Whitelist-Safe? |
|---|----------|----------|-------|---------------|-----------------|
| 1 | **CRITICAL** | ~22 pages (see list below) | Double `\| Brickwise` suffix — plain-string titles already containing `\| Brickwise` have the root layout template `%s \| Brickwise` applied, producing `… \| Brickwise \| Brickwise` | Wrap each affected title in `{ absolute: "…" }` | YES |
| 2 | **CRITICAL** | `app/compare/ark7-vs-lofty/page.tsx` | Title 80 chars raw (+ double suffix = ~92); well over 60-char limit | Shorten to `≤57` chars and use `{ absolute }` | YES |
| 3 | **HIGH** | `app/learn/how-to-invest-in-tokenized-real-estate/page.tsx` | Title 80 chars raw before template is applied | Shorten; use `{ absolute }` | YES |
| 4 | **HIGH** | `app/algorand/page.tsx` | Title `Algorand Ecosystem (30+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise` ≈ 73 chars raw | Shorten to `≤57` chars; use `{ absolute }` | YES |
| 5 | **HIGH** | `app/rankings/[category]/page.tsx` (all 4 categories) | All four category titles are 68–75 chars before template, 80–87 with double suffix | Shorten category titles in `CATEGORY_META`; use `{ absolute }` in generateMetadata | YES |
| 6 | **HIGH** | Multiple compare pages (see list) | Titles 73–80 chars raw, all over 60-char limit | Shorten titles; use `{ absolute }` | YES |
| 7 | **MEDIUM** | `app/learn/what-is-tokenized-real-estate/page.tsx` | OG description has spurious space before comma: `"about tokenized real estate , how it works"` | Remove extra space | YES |
| 8 | **MEDIUM** | `app/market/[date]/page.tsx` | `openGraph` block missing `url` field — canonical URL not surfaced in OG for market reports | Add `url: \`https://brickwise.pro/market/\${u.slug}\`` to openGraph block | YES |
| 9 | **BLOCKER** | All live routes | Live site returns HTTP 403 from sandbox — sitemap.xml, robots.txt, live title rendering, canonical, H1, OG image all unverifiable | Human must verify from unrestricted network; or allowlist sandbox egress IP in WAF | NEEDS HUMAN |

---

## Issue 1 — Double `| Brickwise` detail

Root layout (`app/layout.tsx`) declares `title.template = "%s | Brickwise"`. Pages that export a plain string (not `{ absolute: "…" }`) have this template applied. Pages that already include `| Brickwise` in their string will render as `… | Brickwise | Brickwise`.

**Affected pages** (plain string title, "| Brickwise" already in string):

| File | Raw title string |
|------|-----------------|
| `app/page.tsx` | `"Tokenized Real Estate Scored — Lofty & RealT \| Brickwise"` |
| `app/algorand/page.tsx` | `\`Algorand Ecosystem (${ALGORAND_TOTAL_COUNT}+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise\`` |
| `app/algorand/[slug]/page.tsx` | `\`${project.name} — ${project.category} on Algorand \| Brickwise\`` |
| `app/rankings/[category]/page.tsx` | `\`${meta.title} \| Brickwise\`` |
| `app/market/[date]/page.tsx` | `\`${u.title} \| Brickwise\`` |
| `app/learn/what-is-tokenized-real-estate/page.tsx` | `"What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise"` |
| `app/learn/lofty-review/page.tsx` | `"Lofty Review 2026: Yield, Fees, Risks & How It Works \| Brickwise"` |
| `app/learn/realt-review/page.tsx` | `"RealT Review 2026: Yields, Fees, Risks & Is It Worth It? \| Brickwise"` |
| `app/learn/how-to-invest-in-tokenized-real-estate/page.tsx` | `"How to Invest in Tokenized Real Estate: Step-by-Step Guide (2026) \| Brickwise"` |
| `app/compare/best-fractional-real-estate-platforms/page.tsx` | `"Best Fractional Real Estate Platforms (2026): Honest Comparison \| Brickwise"` |
| `app/compare/realt-vs-fundrise/page.tsx` | `"RealT vs Fundrise (2026): Tokenized Properties vs eREIT Pools \| Brickwise"` |
| `app/compare/arrived-vs-fundrise/page.tsx` | `"Arrived vs Fundrise (2026): Single-Property vs eREIT Comparison \| Brickwise"` |
| `app/compare/ark7-vs-lofty/page.tsx` | `"Ark7 vs Lofty (2026): Regulated Securities vs Tokenized Real Estate \| Brickwise"` |
| `app/compare/best-real-estate-investing-apps/page.tsx` | `"Best Real Estate Investing Apps (2026): Honest Editorial Ranking \| Brickwise"` |
| `app/compare/lofty-vs-arrived/page.tsx` | `"Lofty vs Arrived (2026): Token vs Traditional Rental Comparison \| Brickwise"` |
| `app/city/[city]/page.tsx` | `\`${cityName} Tokenized Real Estate — … \| Brickwise\`` |
| `app/platform/[platform]/page.tsx` | `\`${platformName} Properties — … \| Brickwise\`` |

**Pages correctly using `{ absolute: "…" }` (no issue)**:
- `app/compare/realt-vs-lofty/page.tsx` ✓
- `app/about/page.tsx` ✓
- `app/contact/page.tsx` ✓
- `app/privacy/page.tsx` ✓
- `app/methodology/page.tsx` ✓
- `app/market/page.tsx` ✓
- `app/learn/page.tsx` ✓
- `app/analyzer/layout.tsx` ✓
- `app/property/[id]/page.tsx` (generateMetadata uses `{ absolute }`) ✓

**Fix pattern** (apply to each affected file, preserving the existing title string value):
```typescript
// Before
title: "Some Title | Brickwise",

// After
title: { absolute: "Some Title — Brickwise" },
// Note: also take the opportunity to ensure the title fits ≤60 chars.
// The "—" dash form is preferred over "| Brickwise" per existing correct-page conventions.
```

---

## Issue 3 & 6 — Title length detail

Target: ≤60 chars (including brand suffix). All counts below are the raw string (before double-suffix problem is fixed).

| File | Current length | Current title (truncated) |
|------|---------------|--------------------------|
| `compare/ark7-vs-lofty` | 80 chars | "Ark7 vs Lofty (2026): Regulated Securities vs Tokenized Real Estate \| Brickwise" |
| `learn/how-to-invest` | 80 chars | "How to Invest in Tokenized Real Estate: Step-by-Step Guide (2026) \| Brickwise" |
| `compare/best-fractional-platforms` | 77 chars | "Best Fractional Real Estate Platforms (2026): Honest Comparison \| Brickwise" |
| `compare/best-investing-apps` | 77 chars | "Best Real Estate Investing Apps (2026): Honest Editorial Ranking \| Brickwise" |
| `compare/arrived-vs-fundrise` | 76 chars | "Arrived vs Fundrise (2026): Single-Property vs eREIT Comparison \| Brickwise" |
| `compare/lofty-vs-arrived` | 76 chars | "Lofty vs Arrived (2026): Token vs Traditional Rental Comparison \| Brickwise" |
| `rankings/undervalued` | 75 chars | "Undervalued Tokenized Real Estate Properties — Below Fair Value \| Brickwise" |
| `compare/realt-vs-fundrise` | 73 chars | "RealT vs Fundrise (2026): Tokenized Properties vs eREIT Pools \| Brickwise" |
| `algorand/page` | 73 chars | "Algorand Ecosystem (30+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise" |
| `city/[city]` | ~73 chars (Detroit) | "Detroit Tokenized Real Estate — 45 Properties Ranked by Yield \| Brickwise" |
| `rankings/new-listings` | 72 chars | "New Tokenized Real Estate Listings — Latest Properties Added \| Brickwise" |
| `rankings/buy-signals` | 70 chars | "Active Buy Signals — Best Tokenized Real Estate to Buy Now \| Brickwise" |
| `learn/realt-review` | 70 chars | "RealT Review 2026: Yields, Fees, Risks & Is It Worth It? \| Brickwise" |
| `rankings/highest-yield` | 68 chars | "Highest Yield Tokenized Real Estate Properties Right Now \| Brickwise" |
| `platform/[platform]` | ~69 chars (RealT) | "RealT Properties — 250 Tokenized Real Estate Investments \| Brickwise" |
| `learn/lofty-review` | 65 chars | "Lofty Review 2026: Yield, Fees, Risks & How It Works \| Brickwise" |
| `learn/what-is-tokenized` | 65 chars | "What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise" |

**Note on dynamic city/platform pages**: With ~40 cities and 2 platforms, individual titles vary. The Detroit case is worst at 74 chars for city, and RealT at 69 chars for platform. The title formula in `generateMetadata` should be shortened.

---

## Sitemap & Robots (static-only check)

**Sitemap** (`app/sitemap.ts`): Structure looks correct. Covers: static routes (19), algorand slugs, city slugs (deduped), ranking routes (4), property routes (all `PROPERTIES`), market index + reports, algorand ecosystem entries. No obvious omissions.

Concern: `lastModified: new Date()` is used for most static routes. This means every rebuild will update `lastmod` to the current timestamp for pages that may not have changed, which can dilute crawl priority signals. This is a known minor issue — not actionable in DAILY MONITOR mode.

**Robots** (`app/robots.ts`): All private/auth routes correctly disallowed (`/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`). Sitemap reference correct. `host` is set.

No robots.txt regression (first run, no diff baseline).

---

## Spot-check: font loading, image dimensions, layout shift

From static analysis of `app/layout.tsx`:
- Inter, DM_Mono, DM_Serif_Display all loaded via `next/font/google` with `display: "swap"` ✓ (no manual async stylesheet without `font-display`)
- `NonBlockingStylesheet` component present in layout — implementation not inspected; purpose appears to handle non-critical CSS loading

Images: Cannot verify missing dimension attributes without live rendering. No `<img>` tags were visible in the static page code reviewed (all use Next.js `<Image>` or `<FallbackImg>` components). `<Image>` in Next.js enforces `width`/`height` by default, reducing CLS risk.

---

## robots.txt diff vs yesterday

No previous report exists — first run. Baseline for future diff is the `app/robots.ts` content read today:

- Disallow: /dashboard, /portfolio, /watchlist, /settings, /sign-in, /sign-up, /announce, /seo-dashboard, /crm, /growthos, /api/
- Sitemap: https://brickwise.pro/sitemap.xml
- Host: https://brickwise.pro

---

## Out of scope — needs human

1. **Live site verification** — HTTP 403 from sandbox blocks all live checks. A human or CI job on an unrestricted network must verify: rendered `<title>` output (confirms double-suffix hypothesis), canonical links in `<head>`, H1 count per page, OG image presence (depends on Next.js 16 deep-merge behavior for inherited `openGraph.images`), sitemap.xml HTTP status and URL validity, robots.txt output.

2. **OG image inheritance** — `AGENTS.md` warns this Next.js version has breaking changes. Several pages set `openGraph: { title, description, url }` without `images`. Whether the root layout's `openGraph.images` is inherited (deep merge) or lost (shallow replace) in Next.js 16 is unknown. If images are NOT inherited, the following high-priority pages lack a social sharing image: `app/page.tsx`, `app/about/page.tsx`, `app/algorand/page.tsx`, `app/city/[city]/page.tsx`, `app/learn/what-is-tokenized-real-estate/page.tsx`, `app/learn/lofty-review/page.tsx`, `app/learn/realt-review/page.tsx`, `app/learn/how-to-invest/page.tsx`, all compare pages except `realt-vs-lofty`.

3. **Twitter card inheritance** — Same open question. Pages that don't set `twitter` in their metadata may or may not inherit the root layout's `twitter.card: "summary_large_image"`. If not inherited: About, Algorand, City, Market date, Learn articles, and most Compare pages would lack a Twitter card.

4. **`NonBlockingStylesheet` component** — Purpose unclear from filename; should be reviewed to confirm it doesn't defer critical CSS in a way that causes FOUC or CLS.

5. **Sitemap URL reachability** — Cannot confirm all 687 property/city/algorand URLs in the sitemap return 200. Needs a live crawl.

---

*Generated by seo-routine · 2026-06-10*
