# SEO Daily Monitor — 2026-05-13

**Mode:** DAILY MONITOR (Wed, DOM=13)  
**Run time:** 2026-05-13 04:00 UTC  
**Pages audited:** All PublicShell routes (source analysis)  
**Live-fetch status:** HTTP 403 Forbidden on all URLs — site appears to block automated fetches (bot protection / sandbox network restriction). All findings below are source-code derived and flagged accordingly.

---

## Summary

13 PublicShell page files + 5 dynamic route handlers confirmed. All have `export const metadata` or `generateMetadata`. One architectural SEO bug found affecting ~14 routes: pages that embed `| Brickwise` in a plain `title` string will have the root-layout template (`%s | Brickwise`) appended, producing `… | Brickwise | Brickwise` in rendered `<title>`. This is the highest-priority finding. Title-length overruns co-occur on the same pages.

---

## Findings Table

| # | Sev | Route | Issue | Whitelist-safe? |
|---|-----|-------|-------|-----------------|
| 1 | HIGH | `/` | `title` is a plain string ending `\| Brickwise` — root-layout template appends another `\| Brickwise` → double suffix, ~67 chars rendered. Should use `{ absolute: "…" }`. | ✅ (metadata in PublicShell page) |
| 2 | HIGH | `/algorand` | Same issue — plain string already includes `\| Brickwise`. Base title ~73 chars (over 60 even without double suffix). Rendered title ~85+ chars. | ✅ |
| 3 | HIGH | `/learn/what-is-tokenized-real-estate` | Plain title string includes `\| Brickwise` → double suffix. Base = 64 chars. | ✅ |
| 4 | HIGH | `/learn/lofty-review` | Plain title string includes `\| Brickwise` → double suffix. Base = 66 chars. | ✅ |
| 5 | HIGH | `/learn/realt-review` | Plain title string includes `\| Brickwise` → double suffix. Base = 70 chars (already over 60). | ✅ |
| 6 | HIGH | `/compare/best-fractional-real-estate-platforms` | Plain title `"Best Fractional Real Estate Platforms (2026): Honest Comparison \| Brickwise"` (75 chars base) → double suffix → ~87 chars rendered. | ✅ |
| 7 | HIGH | `/compare/best-real-estate-investing-apps` | Plain title (76 chars base) → double suffix → ~88 chars rendered. | ✅ |
| 8 | HIGH | `/compare/lofty-vs-arrived` | Plain title (75 chars base) → double suffix → ~87 chars rendered. | ✅ |
| 9 | HIGH | `/compare/realt-vs-fundrise` | Plain title (74 chars base) → double suffix → ~86 chars rendered. | ✅ |
| 10 | HIGH | `/compare/ark7-vs-lofty` | Plain title (80 chars base) → double suffix → ~92 chars rendered. | ✅ |
| 11 | HIGH | `/learn/how-to-invest-in-tokenized-real-estate` | Plain title (79 chars base) → double suffix → ~91 chars rendered. | ✅ |
| 12 | HIGH | `/rankings/{highest-yield,buy-signals,undervalued,new-listings}` | `generateMetadata` returns `title: \`${meta.title} \| Brickwise\`` (plain string) → root template appends again. All 4 category titles rendered as "… \| Brickwise \| Brickwise", each well over 80 chars. | ✅ |
| 13 | LOW | `/contact` | No page-specific `openGraph` block — falls back to generic root-layout OG title/description. Contact page is low-traffic; acceptable but improvable. | ✅ (whitelist-safe) |
| 14 | LOW | `/privacy` | Same as contact — no `openGraph` block. | ✅ |
| 15 | LOW | `/privacy` | Meta description = 118 chars (2 chars under the 120-char floor). | ✅ |
| 16 | INFO | Live fetch | All HTTP requests returned 403 Forbidden. Googlebot access unverifiable from this run. Recommend separate manual crawl verification or Search Console check. | N/A |

---

## Correct pages (passing all checks from source)

The following pages correctly use `{ absolute: "…" }` to avoid template double-suffix, have descriptions in range, and have canonical URLs. They serve as the reference pattern:

- `/compare/realt-vs-lofty` — `{ absolute: "RealT vs Lofty (2026): Platform Comparison — Brickwise" }` (54 chars) ✓
- `/analyzer` (layout) — `{ absolute: "Browse N Tokenized Properties — Brickwise" }` ✓
- `/learn` — `{ absolute: "Learn — Tokenized Real Estate Guides & Reviews | Brickwise" }` (58 chars) ✓
- `/market` — `{ absolute: "Tokenized Real Estate Market Overview — Brickwise" }` (49 chars) ✓
- `/about` — `{ absolute: "About Brickwise — Independent Real Estate Analytics" }` (51 chars) ✓
- `/contact` — `{ absolute: "Contact Brickwise — Get in Touch" }` (32 chars) ✓
- `/privacy` — `{ absolute: "Privacy Policy — Brickwise" }` (26 chars) ✓
- `/property/[id]` — `{ absolute: \`${shortName} — ${yield}% Yield | Brickwise\` }` (clamped) ✓
- `/city/[city]` — no `| Brickwise` suffix in the string, so template applies correctly ✓
- `/platform/[platform]` — no `| Brickwise` suffix in the string, so template applies correctly ✓
- `/algorand/[slug]` — no `| Brickwise` suffix in the string, so template applies correctly ✓
- `/market/[date]` — `title: \`${u.title} | Brickwise\`` → same template issue (⚠️ borderline; depends on title content length)

---

## AppShell audit

AppShell usage confirmed on:
- `app/watchlist/page.tsx` — auth-gated ✓
- `app/seo-dashboard/page.tsx` — auth-gated ✓
- `app/not-found.tsx` — non-indexable ✓

No PublicShell routes moved to AppShell. No SSR-breaking changes detected.

---

## robots.ts vs prior report

No prior report exists (first run). Current robots.ts disallows:
`/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`

All look correct. Sitemap declared at `https://brickwise.pro/sitemap.xml`.

---

## Spot-check: 3 pages (image dims, font loading, layout shift hints)

Source review only (live fetch blocked):

1. **Homepage (`/`)** — uses `<FallbackImg>` for property images (custom component with referrer policy). `DM Serif Display`, `DM Mono`, `Inter` fonts loaded via `next/font/google` with `display: swap` ✓. No obvious layout-shift risk from fonts.
2. **`/compare/realt-vs-lofty`** — no images, data-table layout, no layout-shift risk.
3. **`/property/[id]`** — `PropertyHeroImage` component used; could have layout-shift risk if no explicit `width`/`height` dimensions are set. Cannot verify without live fetch. Flag as worth checking.

---

## Needs human

1. **Verify double-title-suffix in production.** Source analysis strongly suggests ~14 routes have `… | Brickwise | Brickwise` in their rendered `<title>`. Confirm in Chrome DevTools or Google Search Console. Fix: change all affected `title: "…"` plain strings to `title: { absolute: "…" }` (whitelist-safe metadata edits in PublicShell pages).

2. **403 on live fetch.** Confirm Googlebot can reach all PublicShell routes. Check Vercel firewall / bot-protection settings are not blocking legitimate crawlers. Review Search Console coverage report.

3. **`/algorand` title too long regardless of double-suffix.** Base title including count and all category labels is ~73 chars. Even with `{ absolute: "…" }` this exceeds 60 chars. Will require editorial shortening.

4. **`/property/[id]` hero image layout shift.** Verify `PropertyHeroImage` sets `width` and `height` props (or uses `fill` with explicit container dimensions) to avoid CLS.

5. **`/market/[date]` borderline.** Market update titles vary in length. With `title: \`${u.title} | Brickwise\`` (plain string) and root-layout template, market update titles could render as "… | Brickwise | Brickwise". Verify.
