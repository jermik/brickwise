# SEO Monitor — 2026-06-12

**Mode**: DAILY MONITOR (Friday, DOM=12)
**Run time**: 04:00 UTC
**Previous report**: None (first run — no baseline to diff)

---

## Summary

26 PublicShell routes inventoried. **9 issues found** across 1 critical, 1 high (needs live verification), 2 medium, and 5 low severity. No regression data available (first run).

Live site returned **HTTP 403 Forbidden** on all WebFetch attempts from sandbox IP. All findings are based on static source-code analysis. Live verification (rendered HTML, canonical links, JSON-LD parse, sitemap/robots.txt) was not possible this run.

---

## Routes Inventoried (PublicShell)

| File | Has metadata? | Notes |
|------|--------------|-------|
| `app/page.tsx` | ✓ | Homepage |
| `app/analyzer/page.tsx` | ✗ | **CRITICAL — `"use client"`, no metadata** |
| `app/algorand/page.tsx` | ✓ | |
| `app/algorand/[slug]/page.tsx` | ✓ | `generateMetadata` |
| `app/city/[city]/page.tsx` | ✓ | `generateMetadata` |
| `app/compare/realt-vs-lofty/page.tsx` | ✓ | Uses `{ absolute }` |
| `app/compare/realt-vs-fundrise/page.tsx` | ✓ | |
| `app/compare/lofty-vs-arrived/page.tsx` | ✓ | |
| `app/compare/arrived-vs-fundrise/page.tsx` | ✓ | |
| `app/compare/ark7-vs-lofty/page.tsx` | ✓ | |
| `app/compare/best-fractional-real-estate-platforms/page.tsx` | ✓ | Title may double-brand |
| `app/compare/best-real-estate-investing-apps/page.tsx` | ✓ | |
| `app/learn/page.tsx` | ✓ | Uses `{ absolute }` |
| `app/learn/what-is-tokenized-real-estate/page.tsx` | ✓ | |
| `app/learn/lofty-review/page.tsx` | ✓ | |
| `app/learn/realt-review/page.tsx` | ✓ | |
| `app/learn/how-to-invest-in-tokenized-real-estate/page.tsx` | ✓ | |
| `app/market/page.tsx` | ✓ | Uses `{ absolute }` |
| `app/market/[date]/page.tsx` | ✓ | `generateMetadata` |
| `app/methodology/page.tsx` | ✓ | **Missing from sitemap** |
| `app/platform/[platform]/page.tsx` | ✓ | `generateMetadata` |
| `app/property/[id]/page.tsx` | ✓ | `generateMetadata`, Twitter images set |
| `app/rankings/[category]/page.tsx` | ✓ | Titles > 60 chars |
| `app/about/page.tsx` | ✓ | Uses `{ absolute }` |
| `app/contact/page.tsx` | ✓ | |
| `app/privacy/page.tsx` | ✓ | |

---

## Findings

| # | Severity | Route | Issue | Suggested Fix | Whitelist-safe? |
|---|----------|-------|-------|---------------|----------------|
| 1 | 🔴 CRITICAL | `/analyzer` | `"use client"` page has no `metadata` export. Next.js silently ignores metadata in Client Components. Page falls back to layout.tsx default title ("Brickwise — Tokenized Real Estate Investment Analytics"). Sitemap priority 0.9. | Extract client UI into `app/analyzer/analyzer-client.tsx`; keep `app/analyzer/page.tsx` as a Server Component that exports metadata and renders `<AnalyzerClient />`. | ❌ Architectural refactor — needs human |
| 2 | 🟠 HIGH | `/`, `/algorand`, `/rankings/*`, `/compare/best-fractional-real-estate-platforms` | **Title double-branding risk.** Root layout defines `title.template: "%s \| Brickwise"`. These pages set plain-string titles that already end with `\| Brickwise`. If template is applied, rendered `<title>` would be "…\| Brickwise \| Brickwise". Cannot confirm without live render (site returns 403 from sandbox). | Change affected pages to use `title: { absolute: "…" }` to bypass the template. For rankings, remove `\| Brickwise` from the `meta.title` string and rely on the template. | ✅ metadata edits in whitelist pages |
| 3 | 🟡 MEDIUM | `/methodology` | Page uses PublicShell, exports proper metadata, has canonical `https://brickwise.pro/methodology`, but is **not included in `app/sitemap.ts`**. This is a high-trust EAT page (methodology transparency is a key brand differentiator). Google may discover it via internal links but has no crawl-priority signal. | Add `{ url: "${base}/methodology", lastModified: new Date(), changeFrequency: "monthly", priority: 0.7 }` to `staticRoutes` in `app/sitemap.ts`. | ✅ `app/sitemap.ts` is whitelisted |
| 4 | 🟡 MEDIUM | `/rankings/*` | All 4 ranking page titles exceed the 60-char guideline. Google truncates to ~58–60 chars in SERPs. Titles: "Highest Yield Tokenized Real Estate Properties Right Now \| Brickwise" (68 chars), "Active Buy Signals — Best Tokenized Real Estate to Buy Now \| Brickwise" (70 chars), "Undervalued Tokenized Real Estate Properties — Below Fair Value \| Brickwise" (75 chars), "New Tokenized Real Estate Listings — Latest Properties Added \| Brickwise" (72 chars). | Shorten each `meta.title` in `CATEGORY_META` to ≤48 chars so `${meta.title} \| Brickwise` ≤60. Examples: "Highest-Yield Tokenized Properties" (34), "Best Tokenized Properties to Buy" (32), "Undervalued Tokenized RE Tokens" (31), "New Tokenized Real Estate Listings" (34). | ✅ metadata in whitelisted PublicShell pages |
| 5 | 🔵 LOW | `/` | Homepage `<meta description>` is ~118 chars with PROPERTIES.length=479 (template: `"${PROPERTIES.length} tokenized properties scored for yield, risk and fair value on Lofty & RealT. Buy/hold/avoid signals updated daily."`). Target is 120–160 chars. Currently 2 chars below minimum. | Append a short trust phrase. Example: change ending to "…signals updated daily. No affiliation, no signup." to reach ~140 chars. | ✅ metadata in whitelisted page.tsx |
| 6 | 🔵 LOW | `/algorand` | Algorand page title is ~72 chars (ALGORAND_TOTAL_COUNT=33): "Algorand Ecosystem (33+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise". If double-branding (finding #2) applies, effective title is 83 chars. | Shorten to ≤55 chars and use `{ absolute }`. Example: `{ absolute: "Algorand Ecosystem (33+ Projects) — Brickwise" }` (46 chars). | ✅ metadata in whitelisted page.tsx |
| 7 | 🔵 LOW | Multiple | 12+ PublicShell pages declare `twitter:card: "summary_large_image"` (via layout.tsx fallback or page-level override) without an explicit `twitter.images` entry. Affected pages: homepage, algorand, rankings, city, platform, methodology, about, market, contact. Property pages are correctly wired with `twitter.images`. Twitter may use the OG image as fallback, but this is not guaranteed. | Add `twitter: { images: ["/opengraph-image"] }` to page-level metadata on affected pages, or verify via live rendering that Twitter is correctly inheriting OG images. | ✅ metadata in whitelisted page.tsx files |
| 8 | 🔵 LOW | Multiple | Several pages set a `openGraph` object without an `images` field — including `/algorand`, `/methodology`, `/about`, `/city/*`, `/platform/*`. In Next.js App Router, a child's `openGraph` object may not inherit `images` from the layout if the child specifies any `openGraph` key. Effect: these pages may render with no `og:image` tag. | Add `images: [{ url: "/opengraph-image", width: 1200, height: 630, alt: "…" }]` to the `openGraph` block of each affected page's metadata. | ✅ metadata in whitelisted page.tsx files |
| 9 | 🔵 LOW | All | **Live site unreachable from sandbox.** All WebFetch calls to `https://brickwise.pro/*` returned HTTP 403 Forbidden. Unable to verify: rendered title/description chars, canonical links in HTML, JSON-LD parse validity, `/sitemap.xml` reachability, `/robots.txt` content, or CWV hints. | Investigate whether Vercel has IP allowlisting that blocks the sandbox's outbound IP range. May need to whitelist the CI/CD range or use a proxy. | ❌ Infrastructure — needs human |

---

## What Was Verified (Static Analysis)

- ✅ `html lang="en"` set in root `app/layout.tsx`
- ✅ All fonts (Inter, DM Mono, DM Serif, General Sans) load with `font-display: swap` — no FOUT risk
- ✅ 25/26 PublicShell pages export `metadata` or `generateMetadata`
- ✅ Global Organization + WebSite JSON-LD in root layout
- ✅ `metadataBase: new URL("https://brickwise.pro")` set — relative `/opengraph-image` paths resolve correctly
- ✅ `app/robots.ts` correctly disallows `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/crm`, `/growthos`, `/api/`, `/announce`, `/seo-dashboard`
- ✅ Property pages (`/property/[id]`) have explicit `twitter.images` set
- ✅ Canonical `alternates.canonical` present on all checked pages
- ✅ Sitemap covers properties, cities, rankings, learn, compare, algorand, market routes
- ✅ No AppShell pages were touched or identified for editing
- ✅ Independence statement ("Not affiliated with Lofty or RealT") visible in copy on homepage
- ✅ No generic AI/growth marketing language found in public pages reviewed

---

## Sitemap Spot-Check (Static)

`app/sitemap.ts` generates these route groups:
- 19 static routes (home, analyzer, compare, learn, platform, algorand, about, contact, privacy)
- 4 ranking routes
- City routes (deduped by slug from PROPERTIES)
- 479 property routes
- Market index + market date routes
- Algorand project routes (33)

**Gap found**: `/methodology` is a real PublicShell page with canonical but missing from sitemap. All other confirmed PublicShell pages appear to be covered.

**No `lastmod` issues detected**: Static routes use `new Date()` (today). Property routes use `new Date(p.lastUpdated)`. Market routes use `new Date(u.date)`.

---

## robots.txt Diff

No previous report to diff against. Current state: allow `/`, disallow list covers all auth/CRM/internal routes. `sitemap:` and `host:` directives present. CRM host blocking also handled at proxy layer (`proxy.ts`).

---

## Spot-Check: 3 Random Pages (Static Analysis)

1. **`/algorand`** — Title too long (72 chars), possible double-brand if template applies. No `twitter.images`. OG `images` not set. H1 present: "The Algorand ecosystem, indexed." JSON-LD: CollectionPage + BreadcrumbList ✓
2. **`/compare/realt-vs-lofty`** — Uses `{ absolute }` correctly. OG `images` set. Title 55 chars ✓. JSON-LD present. 
3. **`/rankings/highest-yield`** — Title 68 chars (over limit). OG images set. No `twitter.images`. JSON-LD: ItemList + BreadcrumbList ✓

---

## Out of Scope — Needs Human

| Item | Reason |
|------|--------|
| Analyzer metadata refactor (Finding #1) | Requires architectural change: extract `"use client"` UI into child component, keep page.tsx as Server Component |
| Title double-branding confirmation (Finding #2) | Needs live render to confirm; sandbox IP blocked by live site |
| Live site 403 investigation (Finding #9) | Infrastructure — Vercel config, IP allowlisting, proxy headers |
| Image dimensions on dynamically-loaded property images | Cannot inspect rendered DOM without live fetch |
| Layout shift (CLS) measurement | Requires browser render / CWV tooling |
| Font fallback metrics | Requires browser render |
| Sitemap XML HTTP status verification | Site returns 403 from sandbox |
