# SEO Monitor — 2026-07-06

**Mode:** DAILY MONITOR (Monday, non-1st)
**Run date:** 2026-07-06 04:00 UTC
**Routine:** seo-routine/brickwise

---

## Summary

- **Pages audited:** 26 PublicShell pages (source audit; live URL fetch blocked — see note)
- **Issues found:** 7 (0 critical, 2 medium, 5 low)
- **Regression vs yesterday:** N/A — first run, no prior report
- **Robots.txt diff:** N/A — no prior report to diff against

> **Network note:** All WebFetch calls to `brickwise.pro` returned HTTP 403. This sandbox's network policy blocks outbound HTTPS to the live site. SEO checks were performed via static source audit only. Live HTML verification (rendered title/description/canonical/lang/JSON-LD/H1 uniqueness) is unconfirmed until a run with live access. This limitation should be flagged to the repo owner.

---

## Findings

| Sev | Route | Issue | Suggested Fix | Whitelist-safe? |
|-----|-------|-------|---------------|-----------------|
| **MEDIUM** | `app/sitemap.ts` | `/methodology` is a PublicShell page with strong JSON-LD and trust-signal content but is **missing from the sitemap**. Googlebot can still discover it via internal links but the sitemap is the primary discovery path. | Add `{ url: \`${base}/methodology\`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.75 }` to `staticRoutes`. | ✅ Yes |
| **MEDIUM** | `/rankings/[category]` | `generateMetadata` returns no `twitter` object. Pages fall back to the root layout's generic Twitter card (`"Brickwise — Tokenized Real Estate Analytics"`). High-traffic ranking pages shared on X will show generic branding instead of page-specific copy (e.g. "Highest Yield Tokenized Real Estate Properties"). | Add a `twitter` block to the `CATEGORY_META` entries or `generateMetadata` function mirroring the `title` + `description`. | ✅ Yes |
| LOW | `app/page.tsx` | Homepage `description` is **118 chars** (target 120–160). Template: `"${PROPERTIES.length} tokenized properties scored for yield, risk and fair value on Lofty & RealT. Buy/hold/avoid signals updated daily."` With 466 properties = 118 chars. | Expand by a few words, e.g. `"for yield, risk, and fair value"` → `"across yield, risk, and fair-value scoring"`. | ✅ Yes |
| LOW | `app/algorand/page.tsx` | `description` is **165 chars** (target ≤160). Excess: 5 chars. Template: `"Discover ${ALGORAND_TOTAL_COUNT}+ projects across the Algorand ecosystem — wallets, DEXs, DeFi, NFT marketplaces, infrastructure, and developer tooling. Curated and updated by Brickwise."` | Drop `"and developer tooling"` or trim `"infrastructure, and developer tooling"` to `"and tooling"`. | ✅ Yes |
| LOW | `app/privacy/page.tsx` | `description` is **118 chars** (target 120–160). No OG block — fully inherits from layout. | Expand description by 2+ chars; optionally add OG block. | ✅ Yes |
| LOW | `app/compare/realt-vs-lofty/page.tsx` | No explicit `twitter` block. Falls back to generic layout Twitter card. This is the site's highest commercial-intent page and the most likely to be shared on X. | Add `twitter: { card: "summary_large_image", title: "RealT vs Lofty (2026): Which Tokenized Real Estate Platform Is Better?", description: "Independent side-by-side comparison..." }` to metadata. | ✅ Yes |
| LOW | `app/city/[city]/page.tsx` (all city pages) | `generateMetadata` has no `twitter` block. City pages (Detroit, Chicago, Memphis, etc.) are long-tail SEO targets and share targets. Generic Twitter card will show when linked. | Add `twitter` block to `generateMetadata`. | ✅ Yes |

---

## Confirmed Good

| Check | Status | Notes |
|-------|--------|-------|
| `<html lang="en">` | ✅ Confirmed in source | `app/layout.tsx:133` |
| All fonts with `display: swap` | ✅ Confirmed | Inter, DM_Mono, DM_Serif_Display all `display: "swap"` |
| `metadataBase` set | ✅ Confirmed | Root layout: `new URL("https://brickwise.pro")` |
| Exactly 1 `<h1>` per public page | ✅ Confirmed in source | Grep across all 26 public pages: 1 H1 each |
| Canonical links present | ✅ Confirmed | All public pages have `alternates: { canonical: ... }` |
| JSON-LD on high-value pages | ✅ Confirmed | Homepage: FAQPage + ItemList; rankings: ItemList + BreadcrumbList; city: Dataset; compare: Article + FAQPage + BreadcrumbList; analyzer: WebPage + SoftwareApplication |
| Robots.ts | ✅ Confirmed | Correctly disallows `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/crm`, `/growthos`, `/api/`, `/announce`, `/seo-dashboard` |
| ClerkProvider inside `<body>` | ✅ Confirmed | `app/layout.tsx:131–135` — wraps inside `<body>`, not `<html>` |
| All PublicShell pages export metadata | ✅ Confirmed | Static pages: direct export; dynamic pages: `generateMetadata` function; analyzer: via `app/analyzer/layout.tsx` |
| OG image set | ✅ Confirmed (most pages) | Most pages specify `images: [{ url: "/opengraph-image" }]` or inherit from root layout |
| Non-blocking font loading | ✅ Confirmed | `NonBlockingStylesheet` component + media=print swap trick for Fontshare stylesheet |

---

## Cannot Confirm (live access required)

| Check | Reason |
|-------|--------|
| Live `<html lang>` in rendered HTML | HTTP 403 from sandbox |
| Canonical tag renders correctly in browser | HTTP 403 from sandbox |
| JSON-LD blocks parse as valid JSON in live page | HTTP 403 (JSON validity confirmed in source; `JSON.stringify()` used in all blocks) |
| `/sitemap.xml` reachable, all URLs return 200 | HTTP 403 from sandbox |
| `/robots.txt` current live content | HTTP 403 from sandbox |
| Missing image `width`/`height` on `<img>` tags | Requires rendered DOM |
| Layout shift (CLS) indicators | Requires browser render |
| OG image resolves correctly (`/opengraph-image`) | Requires live access |

---

## Out of scope — needs human

| Issue | Why |
|-------|-----|
| Live URL verification blocked (HTTP 403 from sandbox) | Network policy issue. This routine cannot verify live SEO from the remote execution environment. Owner should investigate whether the proxy or Vercel access controls block this sandbox's IP range, or switch to a different execution environment. |
| Property page OG images use external property photo URLs (`p.image`) | `app/property/[id]/page.tsx` OG uses raw property photo URLs. These may not be stable over time (third-party images can return 404 or be blocked by hot-link protection). Consider proxying through `/api/og` or a stable CDN. Outside the SEO whitelist (`app/api/` is blacklisted). |
| `/market` page not in sitemap when `MARKET_UPDATES` is empty | `marketIndexRoute` in `sitemap.ts` only adds `/market` conditionally. If `market-updates.json` is empty, `/market` disappears from the sitemap despite the page existing. This is likely intentional but warrants confirmation. |

---

## Spot-Check: 3 Random Pages (source audit)

**1. `/compare/realt-vs-lofty`**
- Title: `"RealT vs Lofty (2026): Platform Comparison — Brickwise"` (54 chars) ✅
- Description: 132 chars ✅
- Canonical: `https://brickwise.pro/compare/realt-vs-lofty` ✅
- H1: 1 ("RealT vs Lofty: Which Tokenized Real Estate Platform Is Better?") ✅
- OG: title, description, type: article, url, image ✅
- Twitter: ❌ none (inherits generic layout card)
- JSON-LD: Article + FAQPage (7 questions) + BreadcrumbList ✅

**2. `/algorand`**
- Title: dynamic with `ALGORAND_TOTAL_COUNT` (33) — est. 60 chars ✅
- Description: **165 chars** ❌ (5 over limit)
- Canonical: `https://brickwise.pro/algorand` ✅
- H1: 1 ✅
- OG: title, description, type: website, url — no explicit image ✅ (inherits from layout)
- Twitter: ❌ none (inherits)
- JSON-LD: CollectionPage + BreadcrumbList ✅

**3. `/city/[city]` (pattern check — Detroit as example)**
- Title: `"Detroit Tokenized Real Estate — N Properties Ranked by Yield | Brickwise"` ✅
- Description: dynamic, varies by city count but well-structured ✅ (est. 145–160 chars)
- Canonical: `https://brickwise.pro/city/detroit` ✅
- H1: 1 ✅
- OG: title, description, type: website, url — **no OG image specified** (inherits `/opengraph-image` from layout, acceptable)
- Twitter: ❌ none (inherits)
- JSON-LD: Dataset + BreadcrumbList ✅

---

## robots.txt (source)

Current `app/robots.ts` disallow list (no prior run to diff against):
```
/dashboard
/portfolio
/watchlist
/settings
/sign-in
/sign-up
/announce
/seo-dashboard
/crm
/growthos
/api/
```
Sitemap: `https://brickwise.pro/sitemap.xml` ✓
Host: `https://brickwise.pro` ✓

No issues found. First-run baseline established.
