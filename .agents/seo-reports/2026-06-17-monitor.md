# SEO Monitor — 2026-06-17

**Mode**: DAILY MONITOR (DOM=17, DOW=3/Wednesday)
**Pages checked**: 23 PublicShell page files (static analysis — see note below)
**Issues found**: 12
**Regression vs yesterday**: N/A (first run — no prior reports to diff against)

---

## Monitoring Limitation

**Live URL monitoring blocked**: All WebFetch requests to `https://brickwise.pro/*` returned HTTP 403 Forbidden. The remote sandbox IP/user-agent is blocked by the site's WAF/proxy layer. This run performs **static source analysis only** — rendered HTML, canonical tags as served, and JSON-LD as parsed by Google cannot be verified. The findings below are derived from reading `app/**/page.tsx` and `app/**/layout.tsx` source files directly.

**Recommended action**: Allowlist the Claude Code remote IP range in Cloudflare / Vercel WAF for monitoring purposes, or set up a dedicated health-check endpoint. Needs human.

---

## Summary

| Category | Count |
|---|---|
| MEDIUM — title >60 chars | 8 pages / templates |
| MEDIUM — description >160 chars | 4 pages |
| LOW — twitter fallback only (no page-specific tags) | 5 pages |
| LOW — property title clamp may still exceed 60 | 1 template |
| INFO — first run, no robots diff available | — |
| INFO — 403 blocks live verification | — |

**Good news**: `html lang="en"` set globally ✓, all three fonts use `display: swap` ✓, Fontshare loaded async ✓, all PublicShell pages have metadata exports ✓, canonical present on all pages ✓, JSON-LD present on all major pages ✓, no AppShell pages mixed into indexable set ✓, `robots.ts` disallows all private routes correctly ✓, sitemap covers all route types ✓.

---

## Findings Table

| Sev | Route / File | Issue | Suggested Fix | Whitelist-safe? |
|---|---|---|---|---|
| MEDIUM | `app/rankings/[category]/page.tsx` | ALL 4 ranking titles exceed 60 chars. Title template is `${meta.title} \| Brickwise`. "highest-yield" = 67 chars, "buy-signals" = 70 chars, "undervalued" = 75 chars, "new-listings" = 72 chars. | Shorten base titles in `CATEGORY_META`. E.g. "Highest Yield Tokenized Real Estate" (35) + " \| Brickwise" = 47 ✓. Drop "Right Now" / "Below Fair Value" suffixes. | Yes |
| MEDIUM | `app/city/[city]/page.tsx` | City title template always exceeds 60 chars. Template: `${cityName} Tokenized Real Estate — ${N} Properties Ranked by Yield \| Brickwise` = 74+ chars for any city. | Shorten to `${cityName} Tokenized Real Estate — ${N} Properties \| Brickwise` (saves ~16 chars). Worst case "Detroit Tokenized Real Estate — 150 Properties \| Brickwise" = 58 ✓. | Yes |
| MEDIUM | `app/platform/[platform]/page.tsx` | Both platform titles exceed 60 chars. Template: `${platformName} Properties — ${N} Tokenized Real Estate Investments \| Brickwise` = ~68 chars. | Shorten to `${platformName}: ${N} Tokenized Properties Ranked \| Brickwise`. "RealT: 400 Tokenized Properties Ranked \| Brickwise" = 51 ✓. | Yes |
| MEDIUM | `app/algorand/page.tsx` | Title `Algorand Ecosystem (30+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise` = 72 chars. | Shorten to `Algorand Ecosystem — 30+ Projects \| Brickwise` = 46 ✓. | Yes |
| MEDIUM | `app/learn/what-is-tokenized-real-estate/page.tsx` | Title 64 chars (>60): "What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise". Description ~197 chars (>160). | Title: "What Is Tokenized Real Estate? A Complete Guide \| Brickwise" = 59 ✓. Description: trim to ≤160 chars. | Yes |
| MEDIUM | `app/learn/lofty-review/page.tsx` | Title 64 chars: "Lofty Review 2026: Yield, Fees, Risks & How It Works \| Brickwise". Description ~167 chars. | Title: "Lofty Review 2026: Yields, Fees & Liquidity \| Brickwise" = 56 ✓. Description: trim to ≤160. | Yes |
| MEDIUM | `app/learn/realt-review/page.tsx` | Title 68 chars: "RealT Review 2026: Yields, Fees, Risks & Is It Worth It? \| Brickwise". Description ~170 chars. | Title: "RealT Review 2026: Real Yields, Fees & Risks \| Brickwise" = 57 ✓. Description: trim to ≤160. | Yes |
| MEDIUM | `app/compare/realt-vs-lofty/page.tsx` (OG title) | OG title "RealT vs Lofty (2026): Which Tokenized Real Estate Platform Is Better? \| Brickwise" = 83 chars. OG titles are truncated in social previews around 60-95 chars — this may be cut mid-sentence. | Shorten OG title to match the page title (55 chars). OG title and meta title can match. | Yes |
| LOW | `app/property/[id]/page.tsx` | `shortName` clamped at 40 chars but the full title template (`shortName` + " — " + "XX.X% Yield \| Brickwise") can reach ~66 chars with 40-char names. The code comment claims ≤60 but the math is off by ~6. | Reduce shortName clamp from 40 to 33: 33 + 3 + 11 + 12 = 59 ✓. | Yes |
| LOW | `app/compare/realt-vs-lofty/page.tsx` | No page-specific `twitter:` metadata. Falls back to global layout twitter card with generic title/description. Low priority since global fallback is present. | Add `twitter: { card: "summary_large_image", title: "...", description: "..." }` to page metadata. | Yes |
| LOW | `app/city/[city]/page.tsx` | No `twitter:` metadata in `generateMetadata`. Falls back to generic global. | Add twitter block to `generateMetadata` return. | Yes |
| LOW | `app/rankings/[category]/page.tsx` | No `twitter:` metadata in `generateMetadata`. Falls back to generic global. | Add twitter block. | Yes |
| INFO | `app/learn/what-is-tokenized-real-estate/page.tsx` | OG description contains `" , "` (space-comma with preceding space) in several places — likely an intentional em-dash avoidance per brand style guide, but reads awkwardly: "tokenized real estate , how it works". | Clarify with owner whether this is intentional style or a formatting artifact. | n/a |

---

## Robots.txt

**Static analysis** — could not verify live `/robots.txt` (403). Based on `app/robots.ts`:
- Correctly disallows: `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`
- Allows all other routes (including all PublicShell pages)
- Sitemap pointer: `https://brickwise.pro/sitemap.xml` ✓
- `/methodology` is NOT disallowed — correct, it is a public PublicShell page ✓
- **No diff available** (first run). Baseline established for future diffs.

---

## Sitemap

**Static analysis** — could not verify live `/sitemap.xml` (403). Based on `app/sitemap.ts`:
- Covers: 19 static routes + 4 ranking routes + N city routes + N property routes + market index + M market routes + P algorand routes
- `lastModified: new Date()` used for most routes — correct, reflects daily rebuild cadence ✓
- Property routes use `new Date(p.lastUpdated)` — accurate ✓
- Market index uses `new Date(MARKET_UPDATES[0].date)` — accurate ✓
- No `/methodology` in sitemap — **MISSING**. The page exists, uses PublicShell, has a canonical, but is absent from the sitemap. Needs human review (could be intentional or an oversight).
- No `/contact` or `/privacy` in sitemap — likely intentional low-priority exclusion. Acceptable.

---

## Spot-checks (3 pages)

Static analysis substituted for live spot-check (403 blocks rendering).

| Page | Image alt | Font display:swap | Layout shift signals |
|---|---|---|---|
| Homepage (`app/page.tsx`) | `alt={bestPick.name}` on hero image (dynamic, will be property address) ✓; fallback avoids properties have hard-coded fallback URL ✓ | All 3 fonts: `display: "swap"` ✓ | Fontshare loaded async via media=print swap ✓; no evidence of render-blocking stylesheets in layout ✓ |
| Analyzer (`app/analyzer/page.tsx`) | No static images directly in page; `<PropertyCard>` and `<PropertyRow>` components — cannot verify without reading component files | Same global fonts ✓ | Client component; SSR limited; may cause CLS from hydration. Acceptable since analyzer is a tool page. |
| Methodology (`app/methodology/page.tsx`) | No `<img>` or `<Image>` tags in the page itself | Same global fonts ✓ | Pure article layout, no dynamic image loading ✓ |

---

## Out of Scope — Needs Human

1. **Live monitoring blocked (HTTP 403)**: WAF/Cloudflare is blocking the routine's execution environment. To restore live HTML verification, the owner needs to either allowlist the remote execution IP range or set up a dedicated health-check endpoint at `/api/seo-check` (auth-free, HEAD-only). Without this, canonical tags, JSON-LD parsing, and actual rendered `<title>` cannot be verified.

2. **`/methodology` missing from sitemap**: The methodology page (`https://brickwise.pro/methodology`) is a PublicShell page with full metadata and canonical but is not included in `app/sitemap.ts`. Unclear if intentional. If it should be indexed, add it to `staticRoutes` in `sitemap.ts`. Owner decision — out of this routine's scope since `sitemap.ts` changes require careful review.

3. **`/learn/what-is-tokenized-real-estate` OG description formatting**: Copy contains `" , "` artifacts that appear to be em-dash avoidances. Owner should confirm the intentional style before this routine edits copy on a high-value learn page.

4. **First run — no historical baseline**: This is the first monitor run. Future runs will have a baseline to diff robots.txt and detect title/description regressions. The 12 issues above represent the opening baseline state.

---

## Prioritized Fix Candidates for Next Sunday (WEEKLY FIX)

These are all whitelist-safe (metadata exports in PublicShell page files):

1. `app/rankings/[category]/page.tsx` — shorten all 4 titles (highest impact, 4 pages fixed in one edit)
2. `app/city/[city]/page.tsx` — shorten title template (affects all city pages)
3. `app/learn/realt-review/page.tsx` — title + description trim
4. `app/learn/lofty-review/page.tsx` — title + description trim
5. `app/learn/what-is-tokenized-real-estate/page.tsx` — title + description trim
6. `app/platform/[platform]/page.tsx` — shorten title template
7. `app/algorand/page.tsx` — shorten title
8. `app/property/[id]/page.tsx` — reduce shortName clamp to 33
