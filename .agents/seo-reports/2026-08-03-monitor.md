# SEO Monitor Report — 2026-08-03

**Mode**: DAILY MONITOR (Monday, DOM=03)
**Branch**: seo/monitor-2026-08-03
**Auditor**: seo-routine (automated)

---

## Summary

**PublicShell pages enumerated**: 26 page.tsx files (+ 2 loading.tsx excluded)
**Issues found**: 15 (3 high, 8 medium, 4 low)
**Regression vs yesterday**: First run — no prior baseline.
**Live fetch**: Blocked. `brickwise.pro` is disallowed by the session egress policy (proxy 403). Audit is source-code-only.

---

## Robots.txt — Current State (no prior diff)

Disallowed paths: `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`

Sitemap declared: `https://brickwise.pro/sitemap.xml`
Host declared: `https://brickwise.pro`

*No prior report to diff against. Baseline captured for next run.*

---

## Findings Table

| # | Severity | Route / File | Issue | Suggested Fix | Whitelist-safe? |
|---|----------|-------------|-------|---------------|-----------------|
| 1 | HIGH | `app/rankings/[category]/page.tsx` — all 4 categories | Titles 68–75 chars, over 60-char limit. E.g. "Highest Yield Tokenized Real Estate Properties Right Now \| Brickwise" = 68 chars | Shorten to ≤60 chars, e.g. "Highest Yield Real Estate Tokens \| Brickwise" | ✅ Yes |
| 2 | HIGH | `app/learn/what-is-tokenized-real-estate/page.tsx` | Description 198 chars, over 160. "Tokenized real estate converts property ownership into blockchain tokens, letting investors buy fractional shares from $50. Learn how it works, typical yields (8–14%), risks, and how to get started." | Trim to ≤160 chars — stop after "from $50. Learn how it works, typical yields (8–14%), and the risks." | ✅ Yes |
| 3 | HIGH | `app/learn/what-is-tokenized-real-estate/page.tsx` | Title 64 chars, over 60. "What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise" | "What Is Tokenized Real Estate? Guide (2026) \| Brickwise" = 56 chars | ✅ Yes |
| 4 | MEDIUM | `app/algorand/page.tsx` | Title 73 chars. "Algorand Ecosystem (30+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise" | "Algorand Ecosystem — 30+ Projects Tracked \| Brickwise" = 54 chars | ✅ Yes |
| 5 | MEDIUM | `app/city/[city]/page.tsx` (all city pages) | Title pattern `${cityName} Tokenized Real Estate — ${N} Properties Ranked by Yield \| Brickwise` exceeds 60 chars for every city with 3+ letter name (e.g. "Detroit Tokenized Real Estate — 120 Properties Ranked by Yield \| Brickwise" = 73 chars) | Shorten to `${cityName} Tokenized RE — ${N} Properties Ranked \| Brickwise` or similar | ✅ Yes |
| 6 | MEDIUM | `app/platform/[platform]/page.tsx` (realt, lofty) | Title pattern `${platformName} Properties — ${N} Tokenized Real Estate Investments \| Brickwise` = ~68 chars for any 3-digit property count | Shorten, e.g. `${platformName} Tokenized Properties — ${N} Listings \| Brickwise` | ✅ Yes |
| 7 | MEDIUM | `app/learn/realt-review/page.tsx` | Title 68 chars. "RealT Review 2026: Yields, Fees, Risks & Is It Worth It? \| Brickwise" | "RealT Review 2026: Yields, Fees & Risks \| Brickwise" = 52 chars | ✅ Yes |
| 8 | MEDIUM | `app/learn/lofty-review/page.tsx` | Title 64 chars. "Lofty Review 2026: Yield, Fees, Risks & How It Works \| Brickwise" | "Lofty Review 2026: Yield, Fees & How It Works \| Brickwise" = 59 chars | ✅ Yes |
| 9 | MEDIUM | `app/learn/realt-review/page.tsx` | Description 170 chars, over 160. "In-depth RealT review based on live data from hundreds of tokenized properties. We analyze actual yields, management fees, vacancy rates, and how RealT compares to Lofty." | Trim to "In-depth RealT review: actual yields, fees, vacancy rates, and how RealT compares to Lofty. Based on live data from hundreds of properties." | ✅ Yes |
| 10 | MEDIUM | `app/learn/lofty-review/page.tsx` | Description ~168 chars, over 160. "Honest Lofty review: $50 minimum, daily USDC payouts, Algorand-based tokens. We analyze real yields, fees, liquidity, and who it's best for. Data from 100+ properties." | Trim to "Honest Lofty review: $50 minimum, daily USDC payouts, Algorand tokens. Real yields, fees, liquidity, and who it's best for. Data from 100+ properties." = 153 chars | ✅ Yes |
| 11 | MEDIUM | `app/sitemap.ts` | `/methodology` route is publicly accessible with PublicShell + metadata but missing from sitemap. Googlebot cannot discover it via sitemap. | Add `{ url: \`${base}/methodology\`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.6 }` to staticRoutes | ✅ Yes |
| 12 | MEDIUM | Multiple pages: `compare/realt-vs-lofty`, `rankings/[category]`, `city/[city]`, `about`, `methodology`, `platform/[platform]`, `algorand/[slug]`, `learn/page.tsx` | Missing `twitter` card in metadata. Root layout provides a fallback but no page-specific title/description for the Twitter card. Pages look less rich in Twitter shares. | Add `twitter: { card: "summary_large_image", title: "...", description: "..." }` to each page's metadata | ✅ Yes |
| 13 | LOW | `app/page.tsx` | Homepage description is ~118 chars with PROPERTIES.length = 460 — just below the 120-char minimum. Borderline; Google may truncate or rewrite. | Append a few words, e.g. append "Free, no signup." to reach ≥120 chars | ✅ Yes |
| 14 | LOW | `app/learn/page.tsx` | No `alternates.canonical` observed in visible code (first 35 lines). If omitted, inherits root layout canonical `https://brickwise.pro` — wrong for `/learn`. | Verify full file and add `alternates: { canonical: "https://brickwise.pro/learn" }` if missing | ✅ Yes |
| 15 | LOW | `app/property/[id]/page.tsx` | `FallbackImg` (plain `<img>`) used with no explicit `width`/`height` attributes. CSS constrains it (`h-[200px]`) but no `loading="lazy"` or intrinsic dimensions — potential CLS on first paint. | See "Needs human" — component is in `components/ui/` (blacklisted) | ❌ No |

---

## JSON-LD Status

Validated schemas by file:

| Page | Schema types | Status |
|------|-------------|--------|
| Root layout | Organization, WebSite (SearchAction) | ✅ Looks valid |
| Homepage | FAQPage, ItemList | ✅ Looks valid |
| `/compare/realt-vs-lofty` | Not read fully yet — assumed Article based on `type: "article"` OG | ⚠️ Full read needed next Sunday |
| `/methodology` | Article, FAQPage | ✅ Looks valid |
| `/about` | AboutPage | ✅ Looks valid |
| `/analyzer` (layout) | WebPage, SoftwareApplication | ✅ Looks valid |

---

## Spot-Check: CWV / Image / Font

| Page | Finding |
|------|---------|
| Homepage (`app/page.tsx`) | `FallbackImg` used on hero property card with no `width`/`height`. CSS contains image in `h-[200px]` div — CLS risk present but likely low in practice. `loading` attribute absent (out of scope, in `components/ui/`). |
| `/market` (market/page.tsx) | `FallbackImg` also used — same concern. |
| Root layout | All fonts (Inter, DM Mono, DM Serif) use `display: "swap"` ✅. Fontshare (General Sans) loaded non-blocking via `media=print` pattern with `&display=swap` in URL ✅. |
| Root layout | `<html lang="en">` present ✅ |

---

## Out of Scope — Needs Human

| Finding | Reason |
|---------|--------|
| `FallbackImg` in `components/ui/fallback-img.tsx` — no `width`/`height` on `<img>`, no `loading="lazy"`. Fix would require adding `width` and `height` props or switching to Next.js `<Image>`. | `components/ui/` is blacklisted. Needs human decision on migration to `<Image>` component. |
| Live crawl audit (canonical match, actual HTTP response codes, real title/description as rendered) | `brickwise.pro` is blocked by egress policy. Cannot do live fetch. Owner should run a live crawl via Screaming Frog or Ahrefs. |
| Sitemap URL reachability (all URLs return 200, lastmod reasonable) | Same egress block — cannot validate live. |

---

## Previous robots.txt state
*None (first run). See "Robots.txt" section above for current baseline.*
