# SEO Monitor — 2026-06-16

**Mode**: DAILY MONITOR (Tuesday, DOM=16)
**Run**: seo-routine v1, first run (no prior reports to diff against)
**Live site access**: HTTP 403 from sandbox — source-code audit only. Rendered HTML, canonical resolution, and robots.txt network check could not be verified.

---

## Summary

28 PublicShell pages audited from source. **18 issues found** across title length, description length, sitemap coverage, and image dimension gaps. No regressions vs yesterday (no prior report). Live site was not reachable from this sandbox — findings are all source-derived.

---

## Findings Table

| # | Sev | Route | Issue | Suggested Fix | Whitelist-safe? |
|---|-----|-------|-------|---------------|-----------------|
| 1 | HIGH | `/methodology` | Page not in `app/sitemap.ts` — won't appear in XML sitemap, reduced crawl discovery | Add `{ url: \`${base}/methodology\`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.7 }` to `staticRoutes` in `app/sitemap.ts` | ✅ Yes |
| 2 | MED | `/rankings/highest-yield` | Title 68 chars (limit 60): "Highest Yield Tokenized Real Estate Properties Right Now \| Brickwise" | Shorten to ≤60, e.g. "Highest-Yield Tokenized Properties Right Now \| Brickwise" (57) | ✅ Yes |
| 3 | MED | `/rankings/buy-signals` | Title 70 chars: "Active Buy Signals — Best Tokenized Real Estate to Buy Now \| Brickwise" | Shorten to ≤60, e.g. "Active Buy Signals — Tokenized Real Estate \| Brickwise" (55) | ✅ Yes |
| 4 | MED | `/rankings/undervalued` | Title 75 chars: "Undervalued Tokenized Real Estate Properties — Below Fair Value \| Brickwise" | Shorten to ≤60, e.g. "Undervalued Tokenized Real Estate Properties \| Brickwise" (57) | ✅ Yes |
| 5 | MED | `/rankings/new-listings` | Title 72 chars: "New Tokenized Real Estate Listings — Latest Properties Added \| Brickwise" | Shorten to ≤60, e.g. "New Tokenized Real Estate Listings \| Brickwise" (47) | ✅ Yes |
| 6 | MED | `/algorand` | Title 72 chars (with 30+ projects): "Algorand Ecosystem (30+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise" | Shorten to ≤60, e.g. "Algorand Ecosystem: 30+ Projects Tracked \| Brickwise" (53) | ✅ Yes |
| 7 | MED | `/platform/[platform]` | Title ~68 chars: "[Name] Properties — [N] Tokenized Real Estate Investments \| Brickwise" | Template too long — shorten to "[Name]: [N] Tokenized Properties \| Brickwise" | ✅ Yes |
| 8 | MED | `/city/[city]` | Title ~74 chars: "[City] Tokenized Real Estate — [N] Properties Ranked by Yield \| Brickwise" | Template too long — shorten to "[City] Tokenized Real Estate — [N] Properties \| Brickwise" | ✅ Yes |
| 9 | MED | `/compare/best-fractional-real-estate-platforms` | Title 75 chars: "Best Fractional Real Estate Platforms (2026): Honest Comparison \| Brickwise" | Shorten to ≤60, e.g. "Best Fractional Real Estate Platforms 2026 \| Brickwise" (55) | ✅ Yes |
| 10 | MED | `/compare/best-real-estate-investing-apps` | Title 76 chars: "Best Real Estate Investing Apps (2026): Honest Editorial Ranking \| Brickwise" | Shorten to ≤60, e.g. "Best Real Estate Investing Apps 2026 \| Brickwise" (49) | ✅ Yes |
| 11 | LOW | `/learn/lofty-review` | Description 167 chars (limit 160): "Honest Lofty review: $50 minimum…" | Trim to 155–160 chars, removing "Data from 100+ properties." clause | ✅ Yes |
| 12 | LOW | `/algorand` | Description 165 chars: "Discover 30+ projects across the Algorand ecosystem…" | Trim "Curated and updated by Brickwise." suffix; saves ~35 chars | ✅ Yes |
| 13 | LOW | `/compare/best-fractional-real-estate-platforms` | Description 196 chars — well over 160 | Trim to ≤160; remove "who each is best for" clause or shorten platform list | ✅ Yes |
| 14 | LOW | `/compare/best-real-estate-investing-apps` | Description 173 chars | Trim to ≤160; remove "best-fit investor" clause | ✅ Yes |
| 15 | LOW | `/` (homepage) | Description ~118 chars (minimum 120) — just under target | Append ". No signup required." (21 chars) → ~139 | ✅ Yes |
| 16 | LOW | `/privacy` | Description 118 chars | Append platform qualifier: "…only. No personal data sold to third parties." → ~145 | ✅ Yes |
| 17 | LOW | `/market/[date]` | OG block missing `url` field — OG crawlers may not attribute canonical URL | Add `url: \`https://brickwise.pro/market/${u.slug}\`` to the openGraph block | ✅ Yes |
| 18 | LOW | `/analyzer` (page.tsx) + all property images | `FallbackImg` component renders `<img>` without `width`/`height` attributes — CLS risk on property cards/hero images (layout shift before image load) | See "Out of scope" — component is in `components/ui/` | ❌ No (component blacklisted) |

---

## Verified OK

| Check | Result |
|-------|--------|
| All 28 PublicShell pages export `metadata` or `generateMetadata` | ✅ Pass |
| All public pages have exactly one `<h1>` | ✅ Pass |
| All fonts loaded with `display: swap` (Inter, DM Mono, DM Serif, General Sans) | ✅ Pass |
| `<html lang="en">` set in root layout | ✅ Pass |
| Sitewide Twitter card (`summary_large_image`) set in root `layout.tsx` — inherited by all pages | ✅ Pass |
| `robots.ts` blocks `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/` | ✅ Pass |
| Global JSON-LD (Organization + WebSite) in root layout | ✅ Pass |
| FAQPage JSON-LD on homepage | ✅ Pass |
| SoftwareApplication + WebPage JSON-LD on `/analyzer` (via layout.tsx) | ✅ Pass |
| `alternates.canonical` present on all static public pages | ✅ Pass (dynamic pages: property, city, ranking, platform, market, algorand/[slug] all set canonical in `generateMetadata`) |
| Sitemap includes property, city, ranking, market, algorand routes | ✅ Pass |
| Sitemap linked in robots.ts | ✅ Pass |
| `metadataBase` set in root layout | ✅ Pass |

---

## Spot-check: 3 Random Pages

**Page 1: `/` (homepage)**
- Images (`FallbackImg`): rendered without `width`/`height` — CLS risk (see issue #18). Parent container uses Tailwind size classes which mitigates partially.
- Fonts: ✅ `display: swap` on all font declarations.
- Layout shift: moderate risk from hero image without explicit dimensions.

**Page 2: `/compare/realt-vs-lofty`**
- Static page, no `FallbackImg` images visible in first 80 lines.
- Fonts: inherits root layout `display: swap` — ✅.
- OG image set (`/opengraph-image` 1200×630) — ✅.

**Page 3: `/algorand`**
- Only 1 width/height reference in entire file — minimal image use.
- Title over 60 chars (see issue #6).
- Description over 160 chars (see issue #12).
- Fonts: ✅.

---

## robots.txt Diff

No prior report to diff against. Baseline recorded from source: `app/robots.ts` disallows `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`. Sitemap declared as `https://brickwise.pro/sitemap.xml`.

Live network fetch returned HTTP 403 — could not confirm rendered output matches source.

---

## Out of Scope — Needs Human

| Item | Why out of scope |
|------|-----------------|
| **Issue #18: `FallbackImg` missing `width`/`height`** | Component is in `components/ui/fallback-img.tsx` — blacklisted. Fix requires adding explicit `width` and `height` props at call sites AND updating the component interface. Scope exceeds a single-file metadata edit. |
| **`analyzer/page.tsx` is `"use client"`** | Entire analyzer page is client-rendered. While metadata is served from `analyzer/layout.tsx` (SSR), the page content is JavaScript-only. Googlebot executes JS, but initial HTML is empty — potential crawl gap for body text and internal links within the analyzer UI. Not fixable without architectural change. |
| **HTTP 403 from sandbox** | Cannot verify: actual rendered `<title>`, `<link rel="canonical">` output, robots.txt content, or sitemap URL reachability. Recommend running a manual check or wiring a site-health monitoring tool (e.g. Ahrefs uptime, Screaming Frog). |
| **`market/[date]` description length** | Description is built from `u.highlights.join(". ")` — variable length depending on JSON data. Some reports may exceed 160 chars or fall below 120 chars. Needs a truncation cap in the `generateMetadata` function (but that touches `app/market/[date]/page.tsx` — whitelist-safe, but flagged because root cause is data-driven). **This one IS whitelist-safe** — adding `description: (u.highlights.join(". ") + ".").slice(0, 158)` in `generateMetadata`. Leaving for weekly fix. |
| **Algorand `[slug]` description length** | `project.shortDescription` drives the meta description — length not bounded. Some project short descriptions may fall under 120 chars. Auditing all 30+ at once needs data review. Needs human to review the Algorand project data. |

---

*Source-only audit. Live site returned HTTP 403 from this sandbox. Next run: 2026-06-17.*
