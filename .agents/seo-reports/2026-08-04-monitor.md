# SEO Monitor — 2026-08-04

**Mode:** DAILY MONITOR (DOM=04, DOW=2/Tuesday)
**Pages checked:** 28 PublicShell routes (code-based audit)
**Live fetch status:** ALL BLOCKED — brickwise.pro returned 403 on all requests from this sandbox IP. Live HTML verification is impossible from this environment. All findings below are code-static analysis only.
**Regression vs yesterday:** No prior reports in `.agents/seo-reports/` — this is the first run.

---

## Summary

28 PublicShell pages audited via source code. 17 confirmed issues found. Live verification was blocked (403 on all WebFetch calls — likely cloud/datacenter IP block by the host or CDN). The most critical unresolved question is whether the root layout title template causes double `| Brickwise` suffixes across ~15 high-traffic pages; this needs a human to confirm with a local build or browser.

---

## Findings

| Sev | Route | Issue | Suggested Fix | Whitelist-safe? |
|-----|-------|-------|---------------|-----------------|
| **HIGH** | 15+ pages (see below) | Possible double `\| Brickwise` suffix — title strings already include `\| Brickwise` but are passed as plain strings, so the root layout template (`"%s \| Brickwise"`) appends a second `\| Brickwise`. Cannot confirm without live fetch. Affected pages: `/`, `/rankings/*`, `/algorand`, `/algorand/[slug]`, `/learn/what-is-tokenized-real-estate`, `/learn/lofty-review`, `/learn/realt-review`, `/learn/how-to-invest-in-tokenized-real-estate`, `/compare/best-real-estate-investing-apps`, `/compare/realt-vs-fundrise`, `/compare/lofty-vs-arrived`, `/compare/best-fractional-real-estate-platforms`, `/compare/ark7-vs-lofty`, `/compare/arrived-vs-fundrise`, `/platform/[platform]`, `/city/[city]` | Each affected page should use `title: { absolute: "..." }` instead of a plain string, OR remove `\| Brickwise` from the string and let the template append it. | Yes — metadata exports in PublicShell pages |
| **HIGH** | `/learn/what-is-tokenized-real-estate` | Title is 64 chars (`"What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise"`) — exceeds 60-char guideline. Likely 76 chars if double-suffix confirmed. | Trim to ≤60 chars, e.g., `"What Is Tokenized Real Estate? Guide (2026)"` with template appending `\| Brickwise`. | Yes |
| **HIGH** | `/learn/how-to-invest-in-tokenized-real-estate` | Title is 77 chars — significantly over limit. Would be 89 with double suffix. | Shorten, e.g., `"How to Invest in Tokenized Real Estate (2026)"` | Yes |
| **HIGH** | `/compare/ark7-vs-lofty` | Title is 79 chars — longest plain-string title found. | Shorten, e.g., `"Ark7 vs Lofty (2026): Securities vs Tokens"` | Yes |
| **HIGH** | `/compare/realt-vs-fundrise` | Title is 73 chars | Shorten, e.g., `"RealT vs Fundrise (2026): Tokenized vs eREIT"` | Yes |
| **HIGH** | `/compare/lofty-vs-arrived` | Title is 75 chars | Shorten, e.g., `"Lofty vs Arrived (2026): Which Platform Wins?"` | Yes |
| **HIGH** | `/compare/best-fractional-real-estate-platforms` | Title is 75 chars | Shorten, e.g., `"Best Fractional Real Estate Platforms (2026)"` | Yes |
| **HIGH** | `/compare/arrived-vs-fundrise` | Title is 75 chars | Shorten | Yes |
| **HIGH** | `/compare/best-real-estate-investing-apps` | Title is 76 chars | Shorten | Yes |
| **MEDIUM** | `/rankings/*` | All 4 ranking page titles exceed 60 chars (`highest-yield`: 68, `buy-signals`: 70, `undervalued`: 75, `new-listings`: 72). Titles are built in `CATEGORY_META` as plain strings with `\| Brickwise` appended in `generateMetadata`. Double-suffix risk too. | Shorten CATEGORY_META title values; use `{ absolute: "..." }` in generateMetadata. | Yes |
| **MEDIUM** | `/learn/lofty-review` | Title is 64 chars | Trim to ≤60 chars | Yes |
| **MEDIUM** | `/learn/realt-review` | Title is 68 chars | Trim to ≤60 chars | Yes |
| **MEDIUM** | 11 content pages | OG image absent: `/learn/what-is-tokenized-real-estate`, `/learn/lofty-review`, `/learn/realt-review`, `/compare/best-real-estate-investing-apps`, `/compare/lofty-vs-arrived`, `/compare/realt-vs-fundrise`, `/compare/best-fractional-real-estate-platforms`, `/compare/ark7-vs-lofty`, `/compare/arrived-vs-fundrise`, `/platform/[platform]`, `/city/[city]`. Pages set `openGraph` without `images`; per Next.js App Router behavior, this replaces the root layout OG entirely — no `og:image` will be rendered. | Add `images: [{ url: "/opengraph-image", width: 1200, height: 630, alt: "..." }]` to each page's openGraph block. | Yes |
| **MEDIUM** | `/learn/what-is-tokenized-real-estate` | OG description and article JSON-LD both contain a typo: `"tokenized real estate , how"` — spurious space before comma appears in both the `openGraph.description` and the `articleSchema.description`. | Remove the space. | Yes |
| **LOW** | `/algorand` (index) | Title includes dynamic `ALGORAND_TOTAL_COUNT` in a plain string. If the count reaches double digits (e.g., 30+), title is 72+ chars. Currently: `"Algorand Ecosystem (${ALGORAND_TOTAL_COUNT}+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise"` | Shorten; remove the NFTs/Wallets/RWA enumeration from title. | Yes |
| **LOW** | `/algorand/[slug]` | OG image absent for all Algorand project pages; `openGraph` set without `images`. | Add default `/opengraph-image` to Algorand project OG. | Yes |
| **LOW** | Sitewide | `lastModified: new Date()` in `sitemap.ts` for all static routes means lastmod is always today's date. Googlebot may over-crawl stable pages. | Use a build-time constant date for infrequently-changing pages (e.g., `/about`, `/privacy`, `/contact`). | Yes — `app/sitemap.ts` is whitelisted |
| **INFO** | All pages | `<html lang="en">` confirmed ✓. All fonts use `display: "swap"` ✓. Canonical links present on all PublicShell pages ✓. robots.ts looks correct ✓. | — | — |
| **INFO** | `app/analyzer/page.tsx` | Uses `"use client"` — metadata served from `analyzer/layout.tsx` instead. This is a valid Next.js App Router pattern; metadata is present and correct. Not an issue. | — | — |

---

## Spot-check: 3 pages (image dims, layout shift, fonts)

**Page 1: `/learn/lofty-review`** — No `<Image>` or `<img>` tags in page source. No layout shift concerns from code. Fonts: Inter/DM_Mono/DM_Serif with `display: "swap"` ✓. General Sans via NonBlockingStylesheet (media=print→all swap pattern) ✓.

**Page 2: `/property/[id]`** — Uses `PropertyHeroImage` component (abstracted). Cannot verify width/height from page.tsx alone. `PropertyHeroImage` should be checked for explicit `width`/`height` or `fill` prop to prevent CLS. Twitter card includes property image URL directly from data.

**Page 3: `/algorand/[slug]`** — Uses `ProjectLogo` component. Cannot verify dimension handling from page.tsx. No other images apparent in page source.

**Font conclusion:** All fonts correctly configured with `display: "swap"` across the codebase ✓.

---

## robots.txt (code-based diff)

No previous report to diff against. Current `robots.ts` disallows:
`/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`

Sitemap declared as `https://brickwise.pro/sitemap.xml` ✓. No flags.

---

## Sitemap (code-based)

`app/sitemap.ts` includes: static routes (19), ranking routes (4), city routes (dynamic, from PROPERTIES), property routes (dynamic, from PROPERTIES), market routes (dynamic), algorand routes (dynamic). 

All routes use `https://brickwise.pro` as base ✓. `lastModified` for most routes is `new Date()` (see LOW finding above). Sitemap itself cannot be fetched (403). No URL malformation detected in code.

---

## Out of scope — needs human

1. **Double `| Brickwise` suffix investigation (CRITICAL)** — Requires running a local build or fetching live HTML. If confirmed, this is a systemic metadata regression affecting 15+ pages including the homepage. The fix is straightforward (wrap titles with `{ absolute: "..." }`), but confirming the bug requires either a browser check or a `next build && next start` test. This is the most urgent item from this run.

2. **Live HTML verification** — All WebFetch calls returned 403. Future runs from this sandbox will have the same limitation. Recommend configuring the monitor to run from an environment with access to the live site, OR adding a `/health` or `/__seo` debug route that bypasses CDN restrictions for internal IPs. Alternatively, test via `curl` from a non-datacenter IP or use a staging URL.

3. **`PropertyHeroImage` and `ProjectLogo` CLS check** — Cannot verify `width`/`height`/`fill` handling from page-level code. A developer should confirm these Next.js Image wrapper components pass explicit dimensions to prevent cumulative layout shift.

4. **H1 count per page** — Cannot verify exactly one `<h1>` per page without live HTML or full component tree traversal. Code-level check would require reading every sub-component file.

5. **JSON-LD validity** — JSON-LD blocks are built server-side as TypeScript objects and serialized to JSON. No parsing issues expected, but live validation against Google's Rich Results Test would confirm rendering.
