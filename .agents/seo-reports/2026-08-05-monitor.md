# SEO Monitor Report — 2026-08-05

**Mode**: DAILY MONITOR (Wednesday, DOM 05)
**Pages enumerated**: 24 PublicShell routes across static and dynamic route segments
**Issues found**: 1 critical, 2 high, 1 medium, 2 low
**Regression vs yesterday**: First run — no baseline exists

---

## ⚠️ Live Check Blocked

`brickwise.pro` returns HTTP 403 from this remote execution environment (cloud runner IP blocked, likely by Vercel's trusted IP policy). All checks are therefore **static source-code analysis only**. The following live checks could NOT be performed:

- Actual rendered `<title>` / `<meta>` / canonical tags in response HTML
- `/sitemap.xml` URL reachability and HTTP 200 verification
- `/robots.txt` diff (no previous report baseline yet)
- Core Web Vitals signals

Static analysis is reliable for metadata exports, JSON-LD blocks, sitemap.ts, and robots.ts. Rendered-HTML verification requires live access (see Needs Human section).

---

## Summary

| Metric | Value |
|--------|-------|
| PublicShell routes confirmed | 24 file paths |
| Pages with `export const metadata` | 18 static route files |
| Pages with `generateMetadata` | 5 dynamic route files (rankings, city, platform, property, algorand/slug, market/date) |
| Pages missing own metadata entirely | 1 (`/analyzer`) |
| Title tags verified over 60 chars | 10+ routes (rankings × 4, city, platform, compare × 1, learn × 3, algorand) |
| Missing from sitemap.ts | 1 (`/methodology`) |
| JSON-LD confirmed | Homepage ×2, compare ×3, learn ×3-4, property ×1, algorand ×2, rankings ×2, city ×3, market/date ×1 |
| `html lang="en"` | ✓ root layout |
| `font-display: swap` | ✓ all fonts (next/font + Fontshare URL param) |

---

## Findings Table

| # | Severity | Route | Issue | Suggested Fix | Whitelist-safe? |
|---|----------|-------|-------|---------------|----------------|
| 1 | 🔴 CRITICAL | `/analyzer` | `"use client"` client component — no `metadata` export, no page-specific title/description/OG. Inherits root layout canonical `https://brickwise.pro` (same as homepage), creating canonical confusion for ~460 property-count tool page | Create `app/analyzer/layout.tsx` with `export const metadata` for the analyzer route (layout.tsx can be a server component even if the page is a client component) | **NO** — file creation outside stated whitelist; needs human decision |
| 2 | 🟠 HIGH | `/methodology` | Page exists with metadata and PublicShell wrapper but is **absent from `app/sitemap.ts`** — Googlebot cannot discover it via XML sitemap | Add `{ url: "${base}/methodology", lastModified: new Date(), changeFrequency: "monthly", priority: 0.75 }` to `staticRoutes` in `app/sitemap.ts` | **YES** — `app/sitemap.ts` is in whitelist |
| 3 | 🟠 HIGH | `/rankings/undervalued` | Title 75 chars (over by 15): `"Undervalued Tokenized Real Estate Properties — Below Fair Value \| Brickwise"` — Google truncates at ~60 chars | `"Undervalued Tokenized Real Estate Properties \| Brickwise"` (57 chars) | **YES** — `generateMetadata` in PublicShell page |
| 4 | 🟠 HIGH | `/compare/best-fractional-real-estate-platforms` | Title 75 chars (over by 15): `"Best Fractional Real Estate Platforms (2026): Honest Comparison \| Brickwise"` | `"Best Fractional Real Estate Platforms 2026 \| Brickwise"` (56 chars) | **YES** — `metadata` export in PublicShell page |
| 5 | 🟠 HIGH | `/rankings/buy-signals` | Title 70 chars (over by 10): `"Active Buy Signals — Best Tokenized Real Estate to Buy Now \| Brickwise"` | `"Active Buy Signals: Best Tokenized Real Estate \| Brickwise"` (59 chars) | **YES** |
| 6 | 🟠 HIGH | `/rankings/new-listings` | Title 72 chars (over by 12): `"New Tokenized Real Estate Listings — Latest Properties Added \| Brickwise"` | `"New Tokenized Real Estate Listings \| Brickwise"` (48 chars) | **YES** |
| 7 | 🟠 HIGH | `/rankings/highest-yield` | Title 68 chars (over by 8): `"Highest Yield Tokenized Real Estate Properties Right Now \| Brickwise"` | `"Highest Yield Tokenized Real Estate Properties \| Brickwise"` (59 chars) | **YES** |
| 8 | 🟠 HIGH | `/algorand` (index) | Title 72 chars (over by 12): `"Algorand Ecosystem (33+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise"` | `"Algorand Ecosystem: 33+ Tracked Projects \| Brickwise"` (54 chars) | **YES** |
| 9 | 🟡 MEDIUM | `/learn/realt-review` | Title 68 chars (over by 8): `"RealT Review 2026: Yields, Fees, Risks & Is It Worth It? \| Brickwise"` | `"RealT Review 2026: Yields, Fees & Risks \| Brickwise"` (53 chars) | **YES** |
| 10 | 🟡 MEDIUM | `/learn/what-is-tokenized-real-estate` | Title 64 chars (over by 4): `"What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise"` | `"What Is Tokenized Real Estate? Guide 2026 \| Brickwise"` (55 chars) | **YES** |
| 11 | 🟡 MEDIUM | `/learn/lofty-review` | Title 64 chars (over by 4): `"Lofty Review 2026: Yield, Fees, Risks & How It Works \| Brickwise"` | `"Lofty Review 2026: Yield, Fees & Risks \| Brickwise"` (52 chars) | **YES** |
| 12 | 🟡 MEDIUM | `city/[city]` pages (all) | Title template generates ~73 chars: `"{City} Tokenized Real Estate — N Properties Ranked by Yield \| Brickwise"` — all city pages over 60 | Template: `"{City} Tokenized Real Estate: {N} Properties \| Brickwise"` (~52 chars for "Detroit") | **YES** — `generateMetadata` in PublicShell page |
| 13 | 🟡 MEDIUM | `platform/[platform]` pages (both) | Title template ~68 chars: `"{Platform} Properties — N Tokenized Real Estate Investments \| Brickwise"` | Template: `"{Platform} Tokenized Real Estate: {N} Properties \| Brickwise"` (~56 chars for "RealT") | **YES** — `generateMetadata` in PublicShell page |
| 14 | 🟡 MEDIUM | Most public pages (~17 routes) | Page-specific Twitter (`twitter:title`, `twitter:description`) absent. Pages inherit root layout's generic card: "Brickwise — Tokenized Real Estate Analytics". When shared to X/Twitter, compare pages, learn pages, rankings, city, and property pages all show generic copy instead of page-specific copy | Add `twitter: { card: "summary_large_image", title: "...", description: "..." }` to each page's metadata export. Only homepage and `/algorand` index currently have page-specific Twitter metadata | **YES** for static metadata pages; **NO** for `/analyzer` (client component) |
| 15 | 🟢 LOW | `/algorand` (index) | Description 165 chars (5 over 160-char cap): `"Discover 33+ projects across the Algorand ecosystem — wallets, DEXs, DeFi, NFT marketplaces, infrastructure, and developer tooling. Curated and updated by Brickwise."` | Trim to 134 chars: `"Discover 33+ projects in the Algorand ecosystem — wallets, DEXs, DeFi, NFTs, and infrastructure. Curated and updated by Brickwise."` | **YES** |
| 16 | 🟢 LOW | `FallbackImg`, `PropertyHeroImage` | `<img>` elements lack HTML `width`/`height` attributes — browser cannot reserve space before CSS loads, potential CLS contribution | Add explicit `width` and `height` attributes matching display dimensions | **PARTIAL** — `PropertyHeroImage` is in `components/property/` (not in blacklist), but `FallbackImg` is in `components/ui/` (blacklisted). Both are outside the explicit whitelist |

---

## Confirmed Good / No Issues

| Check | Result |
|-------|--------|
| `html lang` attribute | ✓ `lang="en"` in root layout |
| `font-display: swap` | ✓ All 3 next/font fonts set `display: "swap"`; Fontshare URL includes `&display=swap` |
| H1 presence | ✓ Exactly one `<h1>` confirmed in all checked pages |
| JSON-LD structured data | ✓ Present on homepage, compare pages, learn pages, property pages, rankings, city pages, algorand, market/date |
| Canonical links | ✓ All pages set page-specific canonicals (explicit in metadata exports, `generateMetadata` returns) |
| Image alt attributes | ✓ All `<img>` calls in public pages pass `alt={p.name}` or equivalent |
| `robots.ts` correctness | ✓ Private routes (`/dashboard`, `/portfolio`, `/watchlist`, `/crm`, `/growthos`, `/api/`, auth flows) correctly disallowed |
| `sitemap.ts` structure | ✓ Covers static routes, rankings, city pages, property pages, market index + updates, algorand projects. Deduplication logic for city slugs present |
| OG image | ✓ Root layout sets `/opengraph-image` (1200×630) with `alt`; per-page OG images referenced in rankings and compare pages |

---

## Out of Scope — Needs Human

### 1. `/analyzer` metadata restructuring (Finding #1 — CRITICAL)

The analyzer page is `"use client"` at the top level, which prohibits a `metadata` export in the same file. Options:

**Option A (recommended):** Create `app/analyzer/layout.tsx` as a server component with the metadata export:
```typescript
// app/analyzer/layout.tsx
import type { Metadata } from "next";
import { PROPERTIES } from "@/lib/data/properties";

export const metadata: Metadata = {
  title: `Tokenized Real Estate Analyzer — ${PROPERTIES.length} Properties | Brickwise`,
  description: `Filter and compare ${PROPERTIES.length} tokenized real estate properties across Lofty and RealT by yield, risk score, and fair value. Buy/hold/avoid signals updated daily.`,
  openGraph: { ... },
  twitter: { ... },
  alternates: { canonical: "https://brickwise.pro/analyzer" },
};

export default function AnalyzerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
```

This requires creating a new file (`app/analyzer/layout.tsx`). File creation is outside the weekly-fix whitelist. Owner decides.

**Option B:** Refactor the page to a server-rendered shell with a client sub-component. Higher effort but architecturally cleaner.

### 2. Live URL verification

brickwise.pro returns HTTP 403 from this sandbox IP. To enable live HTML verification in future runs, allowlist this runner's egress IP on Vercel (or provide a preview URL). The routine cannot check:
- Actual rendered `<title>` and `<meta>` tags
- `/sitemap.xml` HTTP reachability and URL 200 checks
- `/robots.txt` raw output and change detection
- Core Web Vitals (CLS, LCP) from actual page loads

### 3. Image CLS investigation

`FallbackImg` (in blacklisted `components/ui/`) does not forward explicit `width`/`height` props when callers don't pass them. Callers in public pages do not appear to pass dimensions (they rely on CSS `className` for sizing). Owner should audit whether property card thumbnails are contributing to CLS via the Vercel Speed Insights dashboard.

---

## robots.txt — Baseline (First Run)

No prior report exists to diff against. Current state from `app/robots.ts`:

**Disallowed paths**: `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`

**Sitemap declared**: `https://brickwise.pro/sitemap.xml`

Future runs will diff against this baseline.

---

## Spot-check: 3 Pages for CLS / Font / Image Issues

| Page | Images w/ dimensions | Fonts | Notes |
|------|----------------------|-------|-------|
| `/learn/what-is-tokenized-real-estate` | No `<img>` in page file | ✓ next/font swap | No CLS risk from images |
| `/compare/realt-vs-lofty` | No `<img>` in page file | ✓ next/font swap | No CLS risk from images |
| `/rankings/[category]` | No `<img>` in page file | ✓ next/font swap | Property thumbnails rendered via `PropertyRow` component (not in page.tsx directly) |

Note: Property thumbnails use `PropertyRow`/`PropertyCard` components with `<img>` elements sized via CSS `w-10 h-10`. These don't have HTML `width`/`height` attributes. Potential CLS on filtered list pages (`/analyzer`, `/rankings/*`). See Finding #16.

---

*Report generated by seo-routine (static analysis mode). Next scheduled run: 2026-08-06 04:00 UTC.*
