# SEO Monitor Report — 2026-06-13

**Mode:** DAILY MONITOR (Saturday, DOM=13)
**Pages checked:** 26 PublicShell routes (static analysis only — live site returned HTTP 403 from sandbox)
**Issues found:** 14 distinct findings (1 high, 4 medium, 6 low, 3 needs-human)
**Prior report:** None (first run of this routine)
**robots.txt diff:** No prior baseline — current state documented below

---

## Constraints This Run

- **Live site unreachable (HTTP 403):** All checks are static source analysis of `app/` files. Cannot verify rendered HTML, OG image URLs, JSON-LD parsing in-browser, canonical tag output, or actual title rendering.
- **node_modules not installed:** Next.js 16.2.4 docs in `node_modules/next/dist/docs/` unavailable. Title template behavior assessed from code patterns; see Finding #2.

---

## Summary

| Metric | Count |
|--------|-------|
| PublicShell pages with `metadata` export | 25/25 pages (loading.tsx excluded) |
| Pages with canonical tag | 25/25 (some via Next.js auto-inference) |
| Pages with Open Graph | 25/25 |
| Pages with JSON-LD | 10 confirmed (key pages) |
| HTML `lang="en"` | ✓ (root layout) |
| Fonts with `display:swap` | ✓ (all 3 Next.js fonts + Fontshare preload) |
| HIGH issues | 1 |
| MEDIUM issues | 4 |
| LOW issues | 6 |

---

## Findings

### HIGH

| # | Route | Issue | Suggested Fix | Whitelist-safe? |
|---|-------|-------|---------------|-----------------|
| 1 | `app/sitemap.ts` | `/methodology` page missing from sitemap. This is a load-bearing trust page (methodology transparency is core positioning) that uses PublicShell and exports metadata, but is not listed in `staticRoutes`. | Add `{ url: \`${base}/methodology\`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.75 }` to `staticRoutes` in `app/sitemap.ts`. | ✅ Yes |

### MEDIUM

| # | Route | Issue | Suggested Fix | Whitelist-safe? |
|---|-------|-------|---------------|-----------------|
| 2 | Multiple pages | **Potential title duplication.** Root layout sets `template: "%s | Brickwise"`. Many pages export plain-string titles already ending in `"| Brickwise"` (e.g., `"Lofty Review 2026: ... | Brickwise"`). In Next.js metadata, the template applies to all plain-string child titles, potentially rendering as `"...| Brickwise | Brickwise"` (~12–16 extra chars, brand looks broken). Cannot confirm without live site access. **Affected pages:** homepage, rankings (all 4), `learn/lofty-review`, `learn/realt-review`, `learn/what-is-tokenized-real-estate`, `learn/how-to-invest-in-tokenized-real-estate`, `algorand`, platform pages (2), any other page using a plain string title. | Change affected `title: "..."` to `title: { absolute: "..." }` to bypass template. Pages already using absolute (about, compare/realt-vs-lofty, learn/page, analyzer layout, market) are correctly set and unaffected. | ✅ Yes (page.tsx metadata) |
| 3 | `learn/how-to-invest-in-tokenized-real-estate/page.tsx` | Title is 77 chars as written (`"How to Invest in Tokenized Real Estate: Step-by-Step Guide (2026) | Brickwise"`), well over the 60-char limit. With template duplication it would be 89 chars. | Shorten to e.g. `"How to Invest in Tokenized Real Estate (2026) | Brickwise"` (56 chars), use `{ absolute: ... }`. | ✅ Yes |
| 4 | `algorand/page.tsx` | Title is 72 chars (`"Algorand Ecosystem (30+ Projects) — DeFi, NFTs, Wallets, RWA | Brickwise"`). Also description is 165 chars (>160). | Title: `"Algorand Ecosystem (30+ Projects) — DeFi, RWA, Wallets | Brickwise"` (66 chars still over; try `"Algorand Ecosystem — 30+ Projects: DeFi, NFTs, RWA | Brickwise"` = 62 chars). Or use `{ absolute: "Algorand Projects Directory — DeFi, NFTs, Wallets, RWA | Brickwise" }` = 67 chars. Description: trim the last clause. | ✅ Yes |

### LOW

| # | Route | Issue | Suggested Fix | Whitelist-safe? |
|---|-------|-------|---------------|-----------------|
| 5 | `learn/what-is-tokenized-real-estate/page.tsx` | Title is 64 chars (`"What Is Tokenized Real Estate? Complete Guide (2026) | Brickwise"`). Description is ~198 chars (limit 160). | Title: `"What Is Tokenized Real Estate? Guide (2026) | Brickwise"` (55 chars). Description: truncate to ~155 chars. | ✅ Yes |
| 6 | `learn/lofty-review/page.tsx` | Title is 64 chars (`"Lofty Review 2026: Yield, Fees, Risks & How It Works | Brickwise"`). Description is 167 chars (>160). | Title: `"Lofty Review 2026: Yields, Fees & Risks | Brickwise"` (51 chars). Description: trim ~7 chars. | ✅ Yes |
| 7 | `learn/realt-review/page.tsx` | Title is 68 chars (`"RealT Review 2026: Yields, Fees, Risks & Is It Worth It? | Brickwise"`). Description is 170 chars (>160). | Title: `"RealT Review 2026: Yields, Fees & Is It Worth It? | Brickwise"` (62 chars — still 2 over; try `"RealT Review 2026: Yields, Fees & Worth It? | Brickwise"` = 55 chars). Description: trim last clause. | ✅ Yes |
| 8 | `app/city/[city]/page.tsx` | City title template `"${cityName} Tokenized Real Estate — ${N} Properties Ranked by Yield | Brickwise"` produces 70+ chars for every city. Even the shortest city names exceed 60. | Shorten template to e.g. `"${cityName} Tokenized Real Estate — ${N} Properties | Brickwise"` (saves ~16 chars, puts Detroit at ~58 chars). | ✅ Yes |
| 9 | `app/property/[id]/page.tsx` | Description template produces ~83-100 chars (below 120-char minimum) for most of the 460+ property pages. | Expand template to include neighborhood and fair-value fields, e.g. `"${p.platform} rental in ${p.city} — ${p.expectedYield}% net yield, score ${p.overallScore}/100. Risk: ${p.risk}. Fair value: ${p.fairValueStatus}."` | ✅ Yes |
| 10 | `learn/what-is-tokenized-real-estate/page.tsx` | ~15 instances of `" , "` (space-before-comma) in body JSX copy — e.g. `"a fractional share of the property , including"`. Likely artifact of stripped em-dashes. Grammatically unusual and could reduce content quality signals. | Replace ` , ` with `, ` (remove leading space) or `, ` with a proper conjunction where the em-dash was more appropriate. | ✅ Yes |

---

## Spot-Check: 3 Random Pages (CLS + Font)

**Pages spot-checked:** homepage (`app/page.tsx`), `compare/realt-vs-lofty`, `learn/what-is-tokenized-real-estate`

| Check | Result |
|-------|--------|
| Image dimensions on `FallbackImg` | `FallbackImg` uses plain `<img>` without explicit `width`/`height`. Container CSS constrains size so real-world CLS should be low, but it's not best practice. Component is in `components/ui/` (blacklisted). |
| Font-display:swap | ✅ All 3 Next.js fonts (`Inter`, `DM_Mono`, `DM_Serif_Display`) declare `display: "swap"`. Fontshare external font loaded with `display=swap` param via non-blocking `media=print` swap pattern. |
| Async font layout shift | ✅ Fontshare is `rel=preload` → non-blocking; FOUT possible on slow connections but FOUT is preferable to FOIT. |

---

## robots.txt Current State (no prior baseline)

Disallowed paths:
```
/dashboard, /portfolio, /watchlist, /settings (auth surfaces)
/sign-in, /sign-up (auth flows)
/announce, /seo-dashboard (internal)
/crm, /growthos (CRM / GrowthOS)
/api/ (server endpoints)
```

Sitemap declared: `https://brickwise.pro/sitemap.xml`

No anomalies. All expected private surfaces blocked. Note: `/methodology`, `/analyzer`, all public content routes are crawlable.

---

## sitemap.ts Coverage Audit

| Route group | Coverage |
|-------------|----------|
| Static public pages (18) | ✅ Except `/methodology` — see Finding #1 |
| Rankings (4 categories) | ✅ |
| City pages (all cities) | ✅ (dynamic from PROPERTIES) |
| Property pages (460+) | ✅ |
| Market pages (index + per-date) | ✅ |
| Algorand pages (index + per-slug) | ✅ |

---

## Out of Scope — Needs Human

| Finding | Details |
|---------|---------|
| `app/analyzer/page.tsx` is `"use client"` | The entire property list renders client-side. Metadata is correctly set in `app/analyzer/layout.tsx`, but the actual content (property cards, filters) won't appear in server-rendered HTML. Googlebot can execute JS but SSR content is preferred. Fixing requires architecture change (not whitelist-safe). |
| `FallbackImg` has no width/height | Plain `<img>` without explicit dimensions in `components/ui/fallback-img.tsx`. Blacklisted component. Container CSS minimises real CLS but the component should use `width`/`height` or be migrated to Next.js `<Image>`. Engineering fix required. |
| Live site inaccessible from sandbox | HTTP 403 blocks live HTML verification. OG images, JSON-LD parsing, actual canonical output, H1 uniqueness, and title rendering cannot be confirmed. Recommend running a spot-check manually on `brickwise.pro` to verify title template behavior (Finding #2). |
| Rankings page titles include `"| Brickwise"` in string | Rankings `generateMetadata` builds `title: \`${meta.title} | Brickwise\``. If template duplication is confirmed (Finding #2), these strings need refactoring. Titles are also individually over 60 chars. |
