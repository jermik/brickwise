# SEO Monitor — 2026-05-23

**Mode:** DAILY MONITOR  
**Date:** 2026-05-23 (Saturday, DOW=6, DOM=23)  
**Routine:** brickwise seo-routine  
**Live-fetch status:** brickwise.pro returns HTTP 403 from the sandbox runner — all checks are source-level only. Live validation pending human spot-check or next run from an unblocked IP.

---

## Summary

- **Pages checked (source-level):** 26 PublicShell routes
- **Issues found:** 28 (2 high, 20 medium, 6 low)
- **Regression vs yesterday:** N/A — first run; no prior report to diff

---

## Findings Table

| Sev | Route | Issue | Suggested Fix | Whitelist-safe? |
|-----|-------|-------|---------------|-----------------|
| HIGH | `/analyzer` | `"use client"` page has no `metadata` export. No title, description, OG, or Twitter cards. Page is in sitemap at priority 0.9. | Requires server layout restructure (move metadata to a server wrapper or `layout.tsx`). Cannot use static `export const metadata` on a client component. | ❌ No — needs human |
| HIGH | `/` (homepage) | `description` is 118 chars (need ≥ 120). With `PROPERTIES.length = 466` the string is 2 chars short of the lower bound. | Append a short phrase e.g. `" Free, no signup."` to reach 120–135 chars. | ✅ Yes |
| MED | `/learn/what-is-tokenized-real-estate` | `title` is 64 chars (limit: 60). | Shorten to e.g. `"What Is Tokenized Real Estate? Guide (2026) \| Brickwise"` (57 chars). | ✅ Yes |
| MED | `/learn/lofty-review` | `title` is 64 chars (limit: 60). | Shorten to e.g. `"Lofty Review 2026: Yields, Fees & Risks \| Brickwise"` (52 chars). | ✅ Yes |
| MED | `/learn/realt-review` | `title` is 68 chars (limit: 60). | Shorten to e.g. `"RealT Review 2026: Yields, Fees & Risks \| Brickwise"` (52 chars). | ✅ Yes |
| MED | `/algorand` | `title` is 72 chars (limit: 60). | Shorten to e.g. `"Algorand Ecosystem — 31+ Projects Tracked \| Brickwise"` (55 chars). | ✅ Yes |
| MED | `/compare/best-fractional-real-estate-platforms` | `title` is 75 chars (limit: 60). | Shorten to e.g. `"Best Fractional Real Estate Platforms 2026 \| Brickwise"` (55 chars). | ✅ Yes |
| MED | `/compare/lofty-vs-arrived` | `title` is 75 chars (limit: 60). | Shorten to e.g. `"Lofty vs Arrived (2026): Rental Platform Comparison \| Brickwise"` (65)→ `"Lofty vs Arrived 2026: Honest Comparison \| Brickwise"` (53 chars). | ✅ Yes |
| MED | `/compare/best-real-estate-investing-apps` | `title` is 76 chars (limit: 60). | Shorten to e.g. `"Best Real Estate Investing Apps 2026 \| Brickwise"` (49 chars). | ✅ Yes |
| MED | `/learn/how-to-invest-in-tokenized-real-estate` | `title` is 77 chars (limit: 60). | Shorten to e.g. `"How to Invest in Tokenized Real Estate (2026) \| Brickwise"` (59 chars). | ✅ Yes |
| MED | `/learn/what-is-tokenized-real-estate` | `description` is 198 chars (limit: 160). | Trim to 150-char version e.g. `"Tokenized real estate converts property ownership into blockchain tokens. Buy fractional shares from \$50 — learn how it works, yields (8–14%), and risks."` | ✅ Yes |
| MED | `/learn/lofty-review` | `description` is 166 chars (limit: 160). | Trim slightly — remove "Data from 100+ properties" and shorten "who it's best for" clause. | ✅ Yes |
| MED | `/learn/realt-review` | `description` is 170 chars (limit: 160). | Trim — remove "vacancy rates" clause or shorten platform comparison mention. | ✅ Yes |
| MED | `/algorand` | `description` is 165 chars (limit: 160). | Drop "Curated and updated by Brickwise" at end (already implied by domain). | ✅ Yes |
| MED | `/compare/best-fractional-real-estate-platforms` | `description` is 196 chars (limit: 160). | Trim platform list; e.g. `"Independent comparison of Lofty, RealT, Fundrise, Arrived, and Ark7. Covers minimums, liquidity, distribution frequency, and who each platform suits."` (153 chars). | ✅ Yes |
| MED | `/compare/lofty-vs-arrived` | `description` is 181 chars (limit: 160). | Trim e.g. `"Lofty vs Arrived: tokenized (\$50 minimum, daily USDC) vs traditional fractional rentals (\$100 minimum, quarterly payouts). Independent comparison."` (150 chars). | ✅ Yes |
| MED | `/compare/best-real-estate-investing-apps` | `description` is 173 chars (limit: 160). | Trim e.g. `"Lofty, Fundrise, Arrived, Ark7, and RealT compared on minimum, distribution frequency, mobile experience, and best-fit investor profile. Updated 2026."` (155 chars). | ✅ Yes |
| MED | `/learn/how-to-invest-in-tokenized-real-estate` | `description` is 179 chars (limit: 160). | Trim e.g. `"Step-by-step guide to investing in tokenized real estate. Choose a platform, complete KYC, buy your first token, and start earning daily rental income from \$50."` (162→shorten to 158). | ✅ Yes |
| LOW | `/compare/realt-vs-lofty` | Missing `twitter:` card metadata. OG tags present. | Add `twitter: { card: "summary_large_image", title: "...", description: "..." }` to metadata export. | ✅ Yes |
| LOW | `/learn/what-is-tokenized-real-estate` | Missing `twitter:` card metadata. | Add Twitter card to metadata. | ✅ Yes |
| LOW | `/learn/lofty-review` | Missing `twitter:` card metadata. | Add Twitter card to metadata. | ✅ Yes |
| LOW | `/learn/realt-review` | Missing `twitter:` card metadata. | Add Twitter card to metadata. | ✅ Yes |
| LOW | `/methodology` | Missing `twitter:` card metadata. | Add Twitter card to metadata. | ✅ Yes |
| LOW | `/about` | Missing `twitter:` card metadata. | Add Twitter card to metadata. | ✅ Yes |
| LOW | `/algorand` | Missing `twitter:` card metadata. | Add Twitter card to metadata. | ✅ Yes |
| LOW | `/market` | Missing `twitter:` card metadata. | Add Twitter card to metadata. | ✅ Yes |
| LOW | `/rankings/[category]` | Missing `twitter:` card metadata in `generateMetadata`. | Add Twitter card to returned metadata object. | ✅ Yes |
| LOW | `/city/[city]` | Missing `twitter:` card metadata in `generateMetadata`. | Add Twitter card to returned metadata object. | ✅ Yes |

---

## Green / No Issues

| Route | Checks Passed |
|-------|--------------|
| `/` | OG tags ✅, canonical ✅, H1=1 ✅, JSON-LD (FAQPage + ItemList) ✅ |
| `/compare/realt-vs-lofty` | title 54 chars ✅, description 120 chars ✅, canonical ✅, H1=1 ✅, JSON-LD ✅ |
| `/methodology` | title 56 chars ✅, description 150 chars ✅, canonical ✅, H1=1 ✅, JSON-LD ✅ |
| `/about` | title 51 chars ✅, description 145 chars ✅, canonical ✅, H1=1 ✅, JSON-LD ✅ |
| `/market` | title 49 chars ✅, description 130 chars ✅, canonical ✅, H1=1 ✅ |
| `/property/[id]` | dynamic title clamped to ≤60 ✅, description clamped to ≤130 ✅, Twitter ✅, canonical ✅ |
| `html lang` | `lang="en"` in layout.tsx ✅ |
| Fonts | All fonts use `display: swap` ✅ |
| Image alt | FallbackImg always receives `alt` prop in checked pages ✅ |
| Robots policy | Source correct — blocks dashboard, portfolio, crm, api, sign-in/up ✅ |
| H1 counts | All checked pages have exactly 1 `<h1>` ✅ |
| JSON-LD format | All JSON-LD blocks are inline JS objects — runtime serialization via `JSON.stringify` reduces malformed JSON risk ✅ |

---

## Spot-checks (3 pages — image dimensions, CLS hints, fonts)

**Spot-check 1 — `/` (homepage)**  
- Images: `FallbackImg` renders with explicit class + parent has fixed height (`h-[200px]`/`h-[120px]`). No missing `width`/`height` native attributes — relies on CSS. CLS risk is LOW (fixed height containers). Font loading: Next.js google fonts API + fontshare both use `display=swap`. ✅

**Spot-check 2 — `/property/[id]`**  
- Images: `FallbackImg` in a `relative` container with fixed height. No CLS risk. Alt attribute present. No bare `<img>` tags. ✅

**Spot-check 3 — `/compare/realt-vs-lofty`**  
- No images found in source (text-heavy comparison page). No CLS risk from images. Fonts inherited from layout. ✅

---

## Sitemap / Robots

**sitemap.ts (source review):** All major route categories included — static pages, property pages, city pages, rankings, market reports, algorand projects. `lastModified: new Date()` on most routes (acceptable for daily crawl). `/analyzer` included at priority 0.9 — Google will crawl it but will not be able to read a useful title/description due to the missing metadata issue above.

**robots.ts (source review):** Correctly blocks `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`. Sitemap URL correctly set to `https://brickwise.pro/sitemap.xml`. No changes vs baseline (first run).

**Live fetch:** Both `/sitemap.xml` and `/robots.txt` returned HTTP 403 from the sandbox runner. Cannot confirm live delivery. Recommend manual spot-check.

---

## Out of Scope — Needs Human

| Priority | Issue | Why Out of Scope |
|----------|-------|-----------------|
| HIGH | `/analyzer` missing `metadata` — page is `"use client"` | Requires structural change: add a `layout.tsx` metadata wrapper in `app/analyzer/` or refactor the page to use `generateMetadata` in a server parent. The `"use client"` directive makes this a non-trivial change outside the metadata whitelist. |
| MED | Live site returns 403 from sandbox — cannot validate rendered HTML, canonical link injection, or OG tag delivery | Requires access from a non-sandboxed IP; recommend manual browser DevTools check on the 10 highest-priority routes. |
| INFO | `PROPERTIES.length` is 466 in `properties-auto.json` but product-marketing-context.md states 460. Minor discrepancy — confirm which is authoritative. | Data/copy alignment; not an SEO code fix. |
