# SEO Monitor — 2026-05-19

**Mode**: DAILY MONITOR (Tuesday, DOM=19)
**Run by**: seo-routine (automated)
**Pages checked**: 25 PublicShell routes (static source analysis)
**Issues found**: 4 actionable, 2 informational
**Regression vs yesterday**: N/A — first run, no prior report to diff

---

## Summary

Static audit of all 25 PublicShell pages. Live URL verification was **completely blocked** — all requests to `brickwise.pro` returned HTTP 403 from this sandbox environment (WAF or CDN blocking the egress IP). This affects: homepage checks, sitemap.xml, robots.txt, and all per-page WebFetch checks. See "Out of scope — needs human" below.

All 25 PublicShell pages confirmed to export metadata (or have it in a layout sibling). All pages have exactly one H1 tag in source. `html lang="en"` is set in root layout. Font loading uses `display: "swap"` for all three typefaces (Inter, DM Mono, DM Serif Display) and the external Fontshare stylesheet is loaded non-blocking with `preconnect` + `preload` + `media=print` swap. JSON-LD blocks are valid JavaScript objects that serialize correctly via `JSON.stringify()` at render time.

Robots policy matches source: `/robots.ts` correctly disallows auth, CRM, and internal routes while allowing all public surfaces. Sitemap covers static routes, city pages, property pages, rankings, market reports, and Algorand projects. Baseline established — first diff available on next run.

---

## Findings Table

| Sev | Route(s) | Issue | Suggested Fix | Whitelist-safe? |
|-----|----------|-------|---------------|-----------------|
| **HIGH** | `/`, `/learn/*` (4), `/algorand`, `/platform/[platform]`, `/city/[city]`, `/rankings/[category]` | **Probable title double-suffix** — Root layout has `template: "%s \| Brickwise"`. These 9+ routes use plain string titles already ending in `\| Brickwise`, so the rendered title is likely `"... \| Brickwise \| Brickwise"`. Cannot confirm without live access, but documented Next.js behaviour is clear. Compare pages and utility pages correctly use `{ absolute: "..." }` and are unaffected. | Add `{ absolute: "..." }` wrapper to each affected title string, OR remove the trailing `\| Brickwise` from the string and let the template append it once. Must verify live before fixing. | Yes — `metadata` inside PublicShell pages |
| **MEDIUM** | All compare pages (7), all learn pages (5), `/algorand`, `/about`, `/contact`, `/privacy`, `/market`, `/methodology` | **Missing `twitter:` metadata block** — 17 of 25 PublicShell pages have no explicit `twitter:` block. Twitter/X card previews fall back to the root layout's generic `twitter:title` ("Brickwise — Tokenized Real Estate Analytics") rather than the page-specific title and description. Affects social sharing quality. Pages WITH explicit twitter: `/` and `/analyzer`. | Add `twitter: { card: "summary_large_image", title: "...", description: "..." }` to each page's metadata block. Use the existing `openGraph.title` and `openGraph.description` values. | Yes — `metadata` inside PublicShell pages |
| **MEDIUM** | `/compare/realt-vs-lofty` | **Description too short** — ~100 chars. Target is 120–160. The string concatenates a static prefix (~97 chars) plus the property count digit (3 chars) for ~100 chars total. | Expand to: `"Independent comparison of RealT and Lofty tokenized real estate platforms — yields, fees, liquidity, city concentration, and minimum investment. Based on analysis of {N} active properties."` (157 chars with N=3) | Yes — `metadata` inside PublicShell page |
| **LOW** | `/compare/best-fractional-real-estate-platforms` (196), `/compare/arrived-vs-fundrise` (203), `/compare/ark7-vs-lofty` (187), `/compare/realt-vs-fundrise` (184), `/compare/lofty-vs-arrived` (181), `/compare/best-real-estate-investing-apps` (173), `/learn/what-is-tokenized-real-estate` (198), `/learn/how-to-invest-in-tokenized-real-estate` (180), `/learn/realt-review` (170), `/learn/lofty-review` (167), `/algorand` (186) | **Descriptions over 160 chars** — Google truncates these in SERPs. Not harmful but wastes the opportunity to control the displayed snippet. | Trim each to 140–155 chars. Retain the data hook (specific number) in the first clause per the brand formula. | Yes — `metadata` inside PublicShell pages |
| **LOW** | `/privacy` | **Description marginally short** — 118 chars vs 120-char floor. Two chars under target. | Minor; can expand with `"This policy covers data collection, cookies, and how we handle your information when you use Brickwise."` (add 10–15 chars). Low priority. | Yes — `metadata` inside PublicShell page |
| **INFO** | All routes | **Live URL verification blocked** — All WebFetch calls to `brickwise.pro` return HTTP 403. Cannot confirm: rendered title text, canonical link in HTML output, H1 count in rendered DOM, sitemap.xml reachability, robots.txt live content, OG image endpoint (`/opengraph-image`). | Human spot-check from a non-sandboxed browser or curl. Particularly verify finding #1 (title double-suffix) live before the Weekly Fix run applies any changes. | N/A — no code change |

---

## Metadata Audit Detail

### Title lengths (static — template suffix NOT applied)

| Route | Title string (static) | Chars | Status |
|-------|----------------------|-------|--------|
| `/` | "Tokenized Real Estate Scored — Lofty & RealT \| Brickwise" | 57 | ⚠️ Template will extend |
| `/analyzer` | "Browse {N} Tokenized Properties — Brickwise" | ~44 | ✓ `absolute` |
| `/compare/realt-vs-lofty` | "RealT vs Lofty (2026): Platform Comparison — Brickwise" | 54 | ✓ `absolute` |
| `/compare/best-fractional-real-estate-platforms` | "Best Fractional Real Estate Platforms (2026): Honest Comparison \| Brickwise" | 74 | ✓ `absolute` |
| `/compare/lofty-vs-arrived` | "Lofty vs Arrived (2026): Token vs Traditional Rental Comparison \| Brickwise" | 74 | ✓ `absolute` |
| `/compare/realt-vs-fundrise` | "RealT vs Fundrise (2026): Tokenized Properties vs eREIT Pools \| Brickwise" | 72 | ✓ `absolute` |
| `/compare/arrived-vs-fundrise` | "Arrived vs Fundrise (2026): Single-Property vs eREIT Comparison \| Brickwise" | 74 | ✓ `absolute` |
| `/compare/ark7-vs-lofty` | "Ark7 vs Lofty (2026): Regulated Securities vs Tokenized Real Estate \| Brickwise" | 78 | ✓ `absolute` |
| `/compare/best-real-estate-investing-apps` | "Best Real Estate Investing Apps (2026): Honest Editorial Ranking \| Brickwise" | 75 | ✓ `absolute` |
| `/learn` | "Learn — Tokenized Real Estate Guides & Reviews \| Brickwise" | 58 | ✓ `absolute` |
| `/learn/what-is-tokenized-real-estate` | "What Is Tokenized Real Estate? Complete Guide (2026) \| Brickwise" | 64 | ⚠️ Template will extend |
| `/learn/lofty-review` | "Lofty Review 2026: Yield, Fees, Risks & How It Works \| Brickwise" | 64 | ⚠️ Template will extend |
| `/learn/realt-review` | "RealT Review 2026: Yields, Fees, Risks & Is It Worth It? \| Brickwise" | 68 | ⚠️ Template will extend |
| `/learn/how-to-invest-in-tokenized-real-estate` | "How to Invest in Tokenized Real Estate: Step-by-Step Guide (2026) \| Brickwise" | 78 | ⚠️ Template will extend |
| `/algorand` | "Algorand Ecosystem ({N}+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise" | ~75 | ⚠️ Template will extend |
| `/about` | "About Brickwise — Independent Real Estate Analytics" | 51 | ✓ `absolute` |
| `/contact` | "Contact Brickwise — Get in Touch" | 32 | ✓ `absolute` |
| `/privacy` | "Privacy Policy — Brickwise" | 26 | ✓ `absolute` |
| `/market` | "Tokenized Real Estate Market Overview — Brickwise" | 49 | ✓ `absolute` |
| `/methodology` | "Methodology — How Brickwise Scores Tokenized Real Estate" | 56 | ✓ `absolute` |
| `/platform/[platform]` | "{Platform} Properties — {N} Tokenized Real Estate Investments \| Brickwise" | ~72 | ⚠️ Template will extend |
| `/city/[city]` | "{City} Tokenized Real Estate — {N} Properties Ranked by Yield \| Brickwise" | ~73 | ⚠️ Template will extend |
| `/rankings/[category]` | "{meta.title} \| Brickwise" | varies | ⚠️ Template will extend |
| `/property/[id]` | `generateMetadata` dynamic | varies | Needs check |
| `/market/[date]` | `generateMetadata` dynamic | varies | Needs check |

Compare pages (7) use `{ absolute: "..." }` correctly — titles are 54–78 chars, fine for Google's ~580px display constraint.

### Description lengths

| Route | Chars | Status |
|-------|-------|--------|
| `/` | 135 | ✓ |
| `/analyzer` | 144 | ✓ |
| `/compare/realt-vs-lofty` | ~100 | ⚠️ SHORT |
| `/compare/best-fractional-real-estate-platforms` | 196 | ⚠️ LONG |
| `/compare/lofty-vs-arrived` | 181 | ⚠️ LONG |
| `/compare/realt-vs-fundrise` | 184 | ⚠️ LONG |
| `/compare/arrived-vs-fundrise` | 203 | ⚠️ LONG |
| `/compare/ark7-vs-lofty` | 187 | ⚠️ LONG |
| `/compare/best-real-estate-investing-apps` | 173 | ⚠️ LONG |
| `/learn` | 130 | ✓ |
| `/learn/what-is-tokenized-real-estate` | 198 | ⚠️ LONG |
| `/learn/lofty-review` | 167 | ⚠️ LONG |
| `/learn/realt-review` | 170 | ⚠️ LONG |
| `/learn/how-to-invest-in-tokenized-real-estate` | 180 | ⚠️ LONG |
| `/algorand` | 186 | ⚠️ LONG |
| `/about` | 145 | ✓ |
| `/contact` | 127 | ✓ |
| `/privacy` | 118 | ⚠️ SHORT (2 chars under) |
| `/market` | 147 | ✓ |
| `/methodology` | 150 | ✓ |

### JSON-LD validity

All schema blocks pass static validation. Two blocks on the homepage (ItemList) and `/compare/realt-vs-lofty` (Article) use JavaScript runtime expressions (`top5ByScore.length`, `new Date().toISOString()`) — these evaluate correctly at render time via `JSON.stringify()`. No malformed JSON-LD detected.

### Font loading

✓ Inter, DM Mono, DM Serif Display: Next.js `next/font/google` with `display: "swap"` — zero layout shift risk.  
✓ General Sans (Fontshare): Non-blocking via `media=print` swap + `preconnect` + `preload` hint. Correct pattern.  
✓ Noscript fallback present for the async Fontshare stylesheet.

### Images (spot-check: `/`, `/learn/lofty-review`, `/algorand`)

No `<Image>` or `<img>` tags found inline in these pages' JSX — images appear to be encapsulated in components not expanded in this static check. No missing-dimensions issues found in the checked code. OG images use `/opengraph-image` (Next.js dynamic OG) with explicit `width: 1200, height: 630` in all metadata blocks — correct.

### `html lang`

✓ `lang="en"` set on `<html>` in root layout.

### Robots policy (source)

Source matches intent: all auth, CRM, and internal routes disallowed. Public analytics surfaces allowed. Sitemap reference correct. No unexpected additions vs expected baseline.

### Canonical links

All pages set `alternates.canonical` to their canonical URL. Dynamic routes (`/city/[city]`, `/platform/[platform]`, `/rankings/[category]`, `/property/[id]`, etc.) use the correct slug-based canonical. No duplicates or inconsistencies detected in source.

---

## Out of Scope — Needs Human

| Issue | Why out of scope | Action needed |
|-------|-----------------|---------------|
| **Confirm title double-suffix live** | Cannot WebFetch from sandbox; fix would be HIGH impact but cannot verify cause | `curl -s https://brickwise.pro/ \| grep '<title>'` — if output contains "Brickwise \| Brickwise", Issue #1 is confirmed |
| **Sitemap.xml reachability** | 403 from sandbox | Verify `https://brickwise.pro/sitemap.xml` returns 200, all URLs return 200, lastmod dates are within 7 days for daily-refresh routes |
| **Robots.txt live content** | 403 from sandbox | Verify `https://brickwise.pro/robots.txt` matches the source in `app/robots.ts` |
| **OG image endpoint** | 403 from sandbox | Verify `https://brickwise.pro/opengraph-image` returns a valid image (200, Content-Type: image/*) |
| **H1 in rendered HTML** | 403 from sandbox | Verify each public page has exactly one H1 in server-rendered HTML (not just JSX source) |
| **Property/market dynamic metadata** | Dynamic pages not statically analyzable for actual title/desc length | Spot-check 2–3 property pages and 1 market report page for title length and description content |
| **WAF/CDN IP allowlist** | Sandbox egress IP is blocked; if this routine is expected to do live checks, the deployment IP range needs allowlisting | Add sandbox CIDR to Cloudflare/Vercel firewall allow list, or configure the routine to run from an allowed IP |
