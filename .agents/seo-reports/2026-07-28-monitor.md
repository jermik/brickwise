# SEO Monitor — 2026-07-28

**Mode**: DAILY MONITOR (Monday, DOM=28)
**Run**: First run — no prior report to diff against
**Pages checked**: 22 distinct PublicShell routes (14 static, 8 dynamic via `generateMetadata`)
**Issues found**: 10 distinct findings across HIGH/MEDIUM/LOW severity
**Regression vs yesterday**: N/A (first run)

---

## Live Validation Status

> **Sandbox proxy blocks outbound connections to `brickwise.pro:443`.**
> All live-page checks (rendered HTML, canonical URL resolution, sitemap.xml, robots.txt) could not be performed this run. The restriction is sandbox-specific — not an indicator that the live site is down or blocked for real users/crawlers. Source code analysis was used as the primary signal instead.
> Proxy denial logged at 2026-07-28T04:10:18 UTC (`connect_rejected: gateway answered 403`).

---

## PublicShell Route Inventory

| Route | Metadata type | H1 audited |
|-------|--------------|------------|
| `/` | `export const metadata` | via source |
| `/analyzer` | layout.tsx `export const metadata` | via source |
| `/about` | `export const metadata` | not checked |
| `/contact` | `export const metadata` | not checked |
| `/methodology` | `export const metadata` | not checked |
| `/privacy` | `export const metadata` | not checked |
| `/market` | `export const metadata` | not checked |
| `/learn` | `export const metadata` | not checked |
| `/learn/what-is-tokenized-real-estate` | `export const metadata` | via source |
| `/learn/lofty-review` | `export const metadata` | via source |
| `/learn/realt-review` | `export const metadata` | not checked |
| `/learn/how-to-invest-in-tokenized-real-estate` | `export const metadata` | not checked |
| `/compare/realt-vs-lofty` | `export const metadata` | via source |
| `/compare/best-fractional-real-estate-platforms` | `export const metadata` | not checked |
| `/compare/lofty-vs-arrived` | `export const metadata` | not checked |
| `/compare/realt-vs-fundrise` | `export const metadata` | not checked |
| `/compare/arrived-vs-fundrise` | `export const metadata` | not checked |
| `/compare/ark7-vs-lofty` | `export const metadata` | not checked |
| `/compare/best-real-estate-investing-apps` | `export const metadata` | not checked |
| `/algorand` | `export const metadata` | not checked |
| `/rankings/[category]` ×4 | `generateMetadata` | via source |
| `/city/[city]` ×171 | `generateMetadata` | via source |
| `/platform/[platform]` ×2 | `generateMetadata` | not checked |
| `/property/[id]` ×466+ | `generateMetadata` | not checked |
| `/market/[date]` ×N | `generateMetadata` | not checked |
| `/algorand/[slug]` ×33 | `generateMetadata` | not checked |

**Positive**: Every PublicShell route has either a static `metadata` export or a `generateMetadata` function. Root `layout.tsx` provides fallback title template, Twitter card, OG image, and `html lang="en"` (inherited by all).

---

## Findings Table

| # | Severity | Route(s) | Issue | Suggested fix | Whitelist-safe? |
|---|----------|----------|-------|---------------|-----------------|
| 1 | HIGH | `/rankings/[category]` ×4 | All 4 ranking titles exceed 60 chars (68–75 chars). Google rewrites long titles; risks losing keyword focus. | Shorten titles in `CATEGORY_META` in `app/rankings/[category]/page.tsx`. | YES |
| 2 | HIGH | `/city/[city]` ×171 | **All 171** city page titles exceed 60 chars (min 70, max ~90). Template `"{name} Tokenized Real Estate — {N} Properties Ranked by Yield \| Brickwise"` is too long even for the shortest city names. | Shorten template — e.g. `"{name} Tokenized Real Estate — {N} Properties \| Brickwise"` saves ~15 chars. | YES |
| 3 | HIGH | `/learn/what-is-tokenized-real-estate` | Meta description 198 chars (max 160). Full text: *"Tokenized real estate converts property ownership into blockchain tokens, letting investors buy fractional shares from $50. Learn how it works, typical yields (8–14%), risks, and how to get started."* | Trim to ≤160 chars. Suggestion: *"Tokenized real estate converts property ownership into blockchain tokens. Investors buy fractional shares from $50. Learn how it works, typical yields, risks, and how to start."* (174 chars — still needs a small cut). | YES |
| 4 | MEDIUM | `/learn/lofty-review` | Meta description 168 chars (max 160). 8 chars over. | Trim slightly: drop "who it's best for." or abbreviate. | YES |
| 5 | MEDIUM | `/learn/what-is-tokenized-real-estate` | JSON-LD article schema and 2 FAQ answers contain a space before a comma: `"tokenized real estate , how"`, `"the property , including"`, `"(SPV) , typically"`. Also present in OG description. Valid JSON but renders oddly in search snippets. | Remove spurious spaces in `page.tsx` JSON-LD blocks. | YES |
| 6 | MEDIUM | `/` (homepage) | Meta description ~118 chars (min 120). Template: `"${PROPERTIES.length} tokenized properties scored for yield, risk and fair value on Lofty & RealT. Buy/hold/avoid signals updated daily."` — 2 chars short regardless of property count (3-digit numbers). | Expand description by ≥2 chars: e.g. add ", risk," → "scored for yield, risk, and fair value" (note Oxford comma adds 2). | YES |
| 7 | MEDIUM | `/property/[id]` ×466 | Property page descriptions generated as ~86–100 chars, consistently below the 120-char minimum. Format: `"{platform} property in {city} — {yield}% net yield, {score}/100 Brickwise score, €{price} per token."` | Expand template to add `shortDescription` field or a secondary phrase. Complex change with risk of truncation — test carefully. | YES |
| 8 | LOW | `/privacy`, `/contact` | No page-specific OG or Twitter metadata. These inherit root layout fallbacks, which use homepage-level OG title/description. Low priority (utility pages), but OG title mismatch if shared. | Add minimal OG title/description to each. | YES |
| 9 | LOW | `/algorand` | Title `"Algorand Ecosystem (33+ Projects) — DeFi, NFTs, Wallets, RWA \| Brickwise"` = 73 chars. | Shorten: `"Algorand Ecosystem: 33+ Projects — Wallets, DeFi, RWA \| Brickwise"` = 66 chars (still over). Try `"Algorand Projects Directory — DeFi, Wallets, RWA \| Brickwise"` = 60 chars exactly. | YES |
| 10 | LOW | `/` (homepage) — `FallbackImg` | `FallbackImg` in homepage renders `<img>` without explicit `width`/`height` HTML attributes. CSS controls layout (`w-full h-full`) but browser cannot pre-allocate space → potential CLS for property card images. | Add intrinsic dimension attributes to `FallbackImg` component or pass them as props from call sites. | NO — `components/ui/fallback-img.tsx` is blacklisted |

---

## Title Length Summary

| Status | Count | Notes |
|--------|-------|-------|
| Within 60 chars | ~10 routes | Homepage, about, contact, privacy, methodology, market, learn, compare/realt-vs-lofty, algorand/[slug] (dynamic), others |
| Over 60 chars | ~180 routes | All 171 city pages, all 4 ranking pages, `/learn/what-is-tokenized-real-estate` (64), `/learn/lofty-review` (64), `/algorand` (73) |

---

## Description Length Summary

| Status | Count | Notes |
|--------|-------|-------|
| 120–160 chars (ideal) | ~12 routes | compare pages, ranking pages, learn hub, about, methodology, market, etc. |
| Under 120 chars | ~470 routes | Homepage (118), privacy (119), ALL property pages (~86–100 chars) |
| Over 160 chars | 2 routes | `/learn/what-is-tokenized-real-estate` (198), `/learn/lofty-review` (168) |

---

## Sitemap / Robots

**Sitemap**: Could not validate live — sandbox proxy blocks brickwise.pro. Source code reviewed:
- `app/sitemap.ts` returns all static routes, city routes, property routes, ranking routes, market routes, algorand routes.
- `lastModified: new Date()` used for most routes — this is correct for daily-updated content.
- Property pages use `new Date(p.lastUpdated)` — good.
- Sitemap references `https://brickwise.pro/sitemap.xml` in robots.ts — consistent.

**Robots**: Could not validate live. Source code reviewed:
- `app/robots.ts` blocks: `/dashboard`, `/portfolio`, `/watchlist`, `/settings`, `/sign-in`, `/sign-up`, `/announce`, `/seo-dashboard`, `/crm`, `/growthos`, `/api/`.
- Allows `/` for all crawlers.
- Specifies host and sitemap URL.
- No changes from previous run (first run — no diff possible).

---

## Spot-check: Images, Fonts, CLS

**3 pages spot-checked**: `/algorand`, `/compare/realt-vs-lofty`, `/learn/lofty-review`

| Check | Result |
|-------|--------|
| Missing image dimensions | None found in the 3 checked pages. No `<img>` or `<Image>` used (only SVGs for star ratings). |
| Fonts with `font-display: swap` | All 3 Google fonts in `app/layout.tsx` (Inter, DM Mono, DM Serif Display) correctly use `display: 'swap'`. |
| CLS hints | No obvious CLS patterns in checked pages. Homepage (not in spot-check) has `FallbackImg` without dimensions — see Finding #10. |

---

## Out of Scope — Needs Human

| Issue | Route(s) | Why out of scope |
|-------|----------|-----------------|
| `FallbackImg` missing `width`/`height` attributes | `/` (homepage), `/property/[id]` pages | `components/ui/fallback-img.tsx` is in blacklisted `components/ui/` directory |
| Live canonical URL validation | All routes | Sandbox proxy blocks brickwise.pro — requires human with browser access |
| Sitemap URL reachability | `/sitemap.xml` | Same proxy restriction |
| JSON-LD rendering in search console | All routes with JSON-LD | Requires Google Search Console access |
| Property page description template overhaul | `/property/[id]` ×466 | Safe to edit but high-risk change affecting 466 pages — recommend human review before fixing |

---

*Next action: Sunday WEEKLY FIX should prioritize findings #1 (ranking titles) and #2 (city title template) — both affect hundreds of pages and are safe, low-risk edits.*
